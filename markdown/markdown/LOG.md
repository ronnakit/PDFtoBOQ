# Log

## 2569-09-01 (7)

`BACKLOG.md`, `project/116-69/` (ใหม่ — โปรเจกต์ทดสอบที่ 2) — ตัดสินใจเรื่องธุรกิจสำคัญ + พบไฟล์ทดสอบที่เป็น vector จริง

- **ตัดสินใจสำคัญ (เจ้าของโปรเจกต์ยืนยัน):** ไพน้อย phase แรก (ฟรี) **ต้องรันได้เองโดยไม่มีค่าใช้จ่าย AI ต่อครั้ง** เพราะยังไม่มีรายได้ — ปฏิเสธแนวทาง "ให้ AI agent อ่านสดในแชท" ที่เสนอไปก่อนหน้า (ทั้ง 2 แบบ: เจ้าของโปรเจกต์เป็นคนจ่าย API หรือให้ลูกค้าใช้ Claude account ตัวเอง) เพราะเจ้าของโปรเจกต์ไม่อยากพึ่งงบ AI เลยในช่วงที่ยังไม่มีรายได้
- **กลยุทธ์ที่ตกลงกัน:** เช็คก่อนว่าไฟล์ที่ลูกค้าส่งมาเป็น **vector (CAD ส่งออกจริง)** หรือ **ภาพสแกน** (ใช้ `pdf_inspector.py` ที่มีอยู่แล้ว) — ถ้า vector ถอดด้วยโค้ดล้วน (ezdxf/PyMuPDF text extraction) ฟรีไม่มีต้นทุน AI เลย ถ้าเป็นภาพสแกนอย่างเดียว (เหมือน newhouse) ไม่รับถอดฟรีในกรณีนั้น (หรือเก็บเป็นบริการเสริมทีหลัง)
- **ทดสอบไฟล์จริงที่เจ้าของโปรเจกต์ส่งมา** (`116-69 - แบบบ้านชั้นเดียว.pdf`, 47 หน้า, producer "Microsoft: Print To PDF") — `pdf_inspector.py` ยืนยันเป็น `vector_with_text` ทุกหน้า (ไม่ใช่ raster scan เหมือน newhouse) ลองดึงข้อความดิบด้วย PyMuPDF (`get_text`) พบว่า **ตารางเสา/คาน/ฐานราก (S-05, S-09-S-11) ดึงออกมาเป็นตัวหนังสือได้ตรงๆ ไม่ต้องใช้ AI เลย** — เป็นชุดแบบที่ครบกว่า newhouse มาก (A-01..A-24, EE-01..04, **S-01..S-11**, SN-01..08)
- **⚠️ พบปัญหาต้องแก้ก่อนใช้งานจริง:** บางหน้ามีปัญหา**ฟอนต์เพี้ยน** — สระ/วรรณยุกต์ไทยบางตัวดึงออกมาผิด (เช่น "ที่" กลายเป็น "ทีˑ") และหน้า 29 ตัวหนังสือเพี้ยนทั้งหน้า ("ÊÑ­ÅÑ¡É³q...") ต้องทำตารางแปลงรหัสฟอนต์นี้ก่อนถึงจะใช้ดึงข้อมูลจริงได้ — ยังไม่ได้ทำ
- สร้างโปรเจกต์ที่ 2 (`project/116-69/`, ตั้งชื่อตามไฟล์ที่โหลดตามที่เจ้าของโปรเจกต์สั่ง) เก็บไฟล์ PDF ต้นฉบับไว้ในนั้นแล้ว (ก่อนหน้านี้ไฟล์อยู่แค่ใน Downloads ของเจ้าของโปรเจกต์ ไม่มีใน repo เลย)
- ยังไม่ได้เขียนตัวถอดข้อมูลด้วยโค้ดล้วนสำหรับไฟล์นี้ (แค่พิสูจน์ความเป็นไปได้เบื้องต้น) — งานต่อไปคือแก้ปัญหาฟอนต์เพี้ยนก่อน แล้วลองถอดฐานราก-ตอม่อจาก S-05 จริงจัง

## 2569-09-01 (6)

`project/code/python/pynoi.py` (ใหม่), `CLAUDE.md` — ประตูเดียวเรียกทุก module

- เจ้าของโปรเจกต์อยากได้ "สคริปต์เดียว หลาย module" แทนที่จะต้องจำชื่อไฟล์ `extract_*.py` แต่ละไฟล์
- เขียน `pynoi.py` เป็น dispatcher ง่ายๆ: `python pynoi.py <คำสั่ง> [args...]` แล้วส่งต่อ (subprocess) ไปยังไฟล์ module เดิมที่ตรงกับคำสั่งนั้น (new-project, stage0, footing, pier-column, beam, floor, roof, cutting-list, ground-truth) -- ไม่ได้เขียนโค้ดคำนวณใหม่ ผลลัพธ์เหมือนรันไฟล์เดิมตรงๆ ทุกประการ (ทดสอบแล้วว่า `pynoi.py floor` ให้ผลเหมือน `extract_floor_boq.py` ตรงๆ)
- `python pynoi.py --help` แสดงคำสั่งทั้งหมดพร้อมคำอธิบายสั้นๆ

## 2569-09-01 (5)

`project/code/python/project_md_data.py` (ใหม่), `extract_floor_boq.py`, `extract_roof_boq.py`, `project/new house/MD/floor_data.md` (ใหม่), `project/new house/MD/roof_geometry.md` (ใหม่), `new_project.py`, `CLAUDE.md` — แยกข้อมูลเฉพาะโปรเจกต์ออกจากโค้ดคำนวณ

- เจ้าของโปรเจกต์ชี้ว่าข้อมูลที่ยืนยันเฉพาะบ้านหลังหนึ่ง (ห้องพื้น/รูปทรงหลังคา) **ต้องอยู่ในไฟล์ markdown ของโปรเจกต์นั้นเท่านั้น** ไม่ใช่ฝังเป็นค่าคงที่ในตัวสคริปต์ (`ROOM_LIST` เดิมใน `extract_floor_boq.py`, ค่าคงที่ระดับ/กริด/ชายคาเดิมใน `extract_roof_boq.py`) — ส่วนสูตรคำนวณและค่าคงที่ทางฟิสิกส์ (น้ำหนักเหล็ก กก./เมตร) ยังอยู่ในโค้ดได้ตามปกติเพราะใช้ร่วมกันได้ทุกโปรเจกต์
- เขียน `project_md_data.py` — ตัวอ่านไฟล์ markdown แบบง่าย (หัวข้อ `## ` ตามด้วย `- key: value` หรือตาราง) ให้สคริปต์คำนวณอื่นใช้ร่วมกันได้
- ย้าย `ROOM_LIST`+พารามิเตอร์พื้น (`topping_thickness_m`, `s1_cover_m`, ฯลฯ) ออกเป็น `project/new house/MD/floor_data.md`, ย้ายระดับ/กริด/ชายคา/ระยะห่าง/ความยาวสต๊อกของหลังคาออกเป็น `roof_geometry.md`
- **ทดสอบแล้วว่าผลลัพธ์เหมือนเดิมทุกตัวเลข** (HC 169.41ตร.ม., S1 44.86ตร.ม., เหล็กพื้น 438.8+24.1กก., จันทัน 220.83ม./47ชิ้น, cutting list เหมือนเดิมทุกจุด) — พบ typo ของตัวเองระหว่างย้ายข้อมูล (ห้องนอน1 พิมพ์ 36.55 แทน 34.41 ตร.ม.) จับได้เพราะเทียบผลลัพธ์ก่อน-หลังตรงๆ ก่อนเชื่อว่ารีแฟคเตอร์ถูกต้อง
- ปรับ `new_project.py` ให้สร้างโฟลเดอร์ `MD/` เป็นมาตรฐานเพิ่มด้วย
- อัปเดตหมายเหตุใน `CLAUDE.md` ให้ตรงกับสถานะจริง (ไม่มีสคริปต์ไหนฝังข้อมูลเฉพาะโปรเจกต์อีกแล้ว)

## 2569-09-01 (4)

`project/code/python/new_project.py` (ใหม่), `CLAUDE.md` — เตรียมโครงสร้างให้ทดสอบไพน้อยกับโปรเจกต์ใหม่ได้โดยไม่ชนกับ newhouse

- เจ้าของโปรเจกต์จะเอาแบบบ้านหลังใหม่มาทดสอบ ต้องการที่เก็บไฟล์แยกจาก newhouse ชัดเจน + วิธีเอาไฟล์ (PDF/BOQ/CAD) เข้าที่ง่ายๆ
- ตรวจแล้วพบว่าสคริปต์ถอด BOQ ส่วนใหญ่ (`extract_footing_boq.py`, `extract_pier_column_boq.py`, `extract_beam_boq.py`, `extract_foundation_data.py`) **รับ path ไฟล์เป็น argument อยู่แล้ว** ไม่ได้ hardcode "new house" ไว้ในโค้ด — ใช้กับโปรเจกต์ใหม่ได้ทันทีโดยไม่ต้องแก้ มีแค่ `extract_floor_boq.py`/`extract_roof_boq.py` ที่มีข้อมูลเฉพาะโปรเจกต์ (ห้อง/รูปทรงหลังคา) ฝังไว้ ต้องยืนยันใหม่เสมอไม่ว่าจะแก้โครงสร้างโฟลเดอร์อย่างไร
- เขียน `new_project.py` — ถามชื่อโปรเจกต์ใหม่แบบ interactive (input ทีละคำถาม ไม่ต้องจำ syntax argument) สร้างโฟลเดอร์มาตรฐาน (PDF/boq/cad/symbols) + ก็อปปี้ไฟล์ที่ให้ path มาเข้าที่ให้อัตโนมัติ + สร้าง `claude_boq.md` เปล่าเป็นแม่แบบ — มี guard กันสร้างทับโฟลเดอร์เดิมถ้าชื่อซ้ำ
- ทดสอบแล้ว (สร้าง/ก็อปปี้ไฟล์ทดสอบ/ลบทิ้งหลังยืนยันว่าทำงานถูกต้อง ไม่เหลือขยะในรีโพ)
- บันทึกมาตรฐานโฟลเดอร์ต่อโปรเจกต์ไว้ใน `CLAUDE.md` แล้ว

## 2569-09-01 (3)

`project/code/python/extract_roof_boq.py`, `project/new house/claude_boq.md` — เพิ่ม cutting list เหล็กหลังคา (สต๊อก 6.0ม.)

- เจ้าของโปรเจกต์ขอให้คิดปริมาณเหล็กหลังคาเป็น cutting list จริง (FFD ทีละชิ้น, ไม่ใช่หารความยาวรวมด้วยความยาวสต๊อกตรงๆ ตามที่เคยพลาดตอนวิเคราะห์ไฟล์ 3D เมทัลชีทก่อนหน้านี้ในวันเดียวกัน) — เพิ่มฟังก์ชัน `cutting_list_report()` ใน `extract_roof_boq.py`
- แยก pack ตามกลุ่มเหล็กที่ profile ตรงกันเป๊ะเท่านั้น (2-C150x50=อะเส+ดั้ง, 2-C100x50=อกไก่, C100x50เดี่ยว=จันทัน — อกไก่กับจันทันคนละ profile แม้ขนาดหน้าตัดชื่อคล้ายกัน ห้ามรวมสต๊อกกัน)
- ผล: 2-C150x50 ต้องซื้อ 23 ท่อน (ของเสีย 19.4%), 2-C100x50 ต้องซื้อ 5 ท่อน (3.7%), C100x50เดี่ยว (เฉพาะจันทัน 28/47 ชิ้นที่ไม่เกิน 6ม.) 26 ท่อน (33.7%)
- **พบจันทัน 19/47 ชิ้น (ฝั่งตะวันตกจั่วหลัก) ยาว 6.177ม. เกินสต๊อก 6.0ม.เล็กน้อย** — ไม่ใส่ในตาราง cutting-list เพราะรอยต่อกลางช่วงจันทันไม่เหมาะทางโครงสร้าง ต้องสั่งสต๊อกยาวพิเศษหรือตัดจากสต๊อก 12ม.แทน (ยังไม่ยืนยันวิธีแก้กับเจ้าของโปรเจกต์)
- ระแนงยังทำ cutting list ไม่ได้ (มีแค่ความยาวรวมประมาณจากพื้นที่ ไม่มีความยาวทีละเส้น)

## 2569-09-01 (2)

`project/code/python/extract_roof_boq.py` (ใหม่), `project/new house/claude_boq.md`, `BACKLOG.md` — ปิดเคสโครงสร้างหลังคา หมวดโครงสร้างครบจริงแล้ว

- **เรขาคณิตหลังคาซับซ้อนกว่าที่คาด ต้องยืนยันกับเจ้าของโปรเจกต์หลายรอบ** จนได้ข้อสรุป: เป็นจั่วเต็มความยาว 2 หลัง (สันหลักที่คอลัมน์2, สันปีกส่วนต่อที่กึ่งกลางแถวA-B) มาซ้อนทับกัน เกิดตะเข้จุดเดียว (ไม่ใช่ปั้นหยาเต็มรูปแบบตามที่เข้าใจตอนแรก) — จุดเปลี่ยนสำคัญคือเจ้าของโปรเจกต์ส่งภาพ 3D SketchUp massing model มาช่วยยืนยันรูปทรง แทนที่จะพยายามอ่านตำแหน่งเส้นทแยงจากภาพสแกน S-03/S-04 เอง (พิสูจน์อีกครั้งว่าอ่านตำแหน่ง/ระยะจากภาพสแกนความหนาแน่นสูงไม่แม่นยำ — ต้องขอยืนยันเป็นรอบๆ)
- **ระยะรางตะเข้ (3.5ม.จากคอลัมน์2) ยืนยันตรงกับสูตรคำนวณเอง** (=คอลัมน์4-1.0ม.ชายคายื่น-คอลัมน์2) ทำให้มั่นใจว่าโมเดลเรขาคณิตถูกต้อง
- **ผลคำนวณสุดท้าย:** จันทัน 220.83ม./47ชิ้น, ตะเข้ราง 9.35ม./2ชิ้น, อกไก่ 28.90ม., ดั้ง 31ชิ้น, อะเส 39.90ม., ระแนง ~639.5ม. (ประมาณจากพื้นที่)
- **เทียบกับไฟล์ 3D component export ที่เจ้าของโปรเจกต์ส่งมา (`newhouse เหล็กหลังคา.xlsx`) ไม่ใช่ ground-truth ที่ใช้ได้** — เจ้าของโปรเจกต์ยืนยันว่าหลังคาที่สร้างจริงเปลี่ยนเป็นเมทัลชีท (เหล็กเดิมเอาไปทำโรงจอดรถต่อ) คนละแบบกับที่ระบุใน S-03/S-04 โดยสิ้นเชิง — ไม่บันทึกเป็นข้อผิดพลาดของ BOQ ใน `08-ground-truth-boq-validation.md` ตามที่เจ้าของโปรเจกต์สั่ง
- **บทเรียนย่อยเรื่อง cutting list:** ระหว่างวิเคราะห์ไฟล์ 3D พบว่าการหารความยาวรวมด้วยความยาวสต๊อกตรงๆ (total÷6ม.) ให้ผลผิดจากการนับทีละชิ้นจริง (ceil ต่อชิ้นก่อนค่อยรวม) มากถ้ามีชิ้นยาวเกินความยาวสต๊อก — ยืนยันหลักการเดิมที่ใช้ใน `combine_cutting_list.py` อยู่แล้ว
- **สถานะ:** หมวดโครงสร้าง (ฐานราก+ตอม่อ-เสา+คาน+พื้น+หลังคา) ครบทุกหมวดย่อยแล้วจริง — เจ้าของโปรเจกต์แจ้งว่าจะนำโครงการไปทดลองใช้งานจริงและเริ่มเขียน/เผยแพร่ไพน้อยต่อ

## 2569-09-01 (1)

`03-ai-boq-procedure.md`, `BACKLOG.md`, เวิร์กโฟลว์ไพน้อย (artifact) — แก้ไขสำคัญ: หมวดโครงสร้างยังไม่ครบ 100% ตามที่เคยประกาศ + วางขอบเขตแกนหลัก vs ส่วนเสริม

- เขียนแผนผังเวิร์กโฟลว์ไพน้อยเป็น artifact ให้เจ้าของโปรเจกต์ดู ระหว่างแก้ไขตามฟีดแบ็กพบว่า **ลืมหมวด "5.0 โครงสร้างหลังคา" ไปทั้งหมวด** — เทียบ BOQ จริง (`Struc` sheet) พบว่า item 5.0 = 553,464 บาท **ใหญ่กว่าฐานราก+ตอม่อ-เสา+คาน+พื้นรวมกัน** เป็นรายการเดี่ยวที่ใหญ่สุดของทั้งหมวดโครงสร้าง แต่เซสชันก่อนหน้ายังไม่แตะเลย — ถอนคำประกาศ "ครบ 100%" ใน BACKLOG.md แล้ว เพิ่มเป็นรายการค้างใหม่ (ไม่รวมแผ่นมุงหลังคา ซึ่งเป็นงานสถาปัตย์/finish)
- **เพิ่มกฎใหม่ใน Stage 0:** ต้องตรวจความครบถ้วนของแบบ (ทุกรหัสบนผังมีแบบขยาย/schedule รองรับไหม) ก่อนเริ่มถอดจริง ถ้าขาดให้ทำรายการแจ้งกลับแทนถอดต่อแล้วต้องทิ้งงาน
- **วางขอบเขต "แกนหลัก vs ส่วนเสริม" ของไพน้อยชัดเจน (ยืนยันจากเจ้าของโปรเจกต์):** แกนหลัก (จะเผยแพร่ฟรี) = ถอดปริมาณอย่างเดียว แสดงผลแยกทีละรายการย่อยเสมอไม่รวมกัน (แม้ของเสียดูสูงกว่าที่ควร เพื่อให้ลูกค้าตรวจเองได้) — ส่วนเสริม (แยกออกไปชัดเจน ไม่บล็อกการเผยแพร่) = รวม cutting list ข้ามหมวด (งานบริหารก่อสร้าง), เทียบ BOQ จริง (งานสอบเทียบ), ใส่ราคา (งานเพิ่ม)

## 2569-08-31 (5)

`project/new house/claude_boq.md`, `BACKLOG.md` — ปิดเคสสุดท้ายของหมวดโครงสร้าง: คานคอดินลอยใต้ทางลาดคนพิการ

- **พบตำแหน่งจริงในแบบ A-01 (ผังบริเวณ)** ตรงกับที่เจ้าของโปรเจกต์ชี้ (ทางลาดโค้งมุมบ้าน ใกล้ที่จอดรถ) — แต่แบบไม่มีตัวเลขความยาวทางลาดกำกับเลย (เป็นเส้นโค้ง วัดจากภาพสแกนไม่แม่นยำ)
- **เจ้าของโปรเจกต์ให้คำนวณย้อนกลับจากกฎหมายแทนวัดจากแบบ:** ความสูง 1.2ม.(ค่าเดียวกับตอม่อทั้งโครงการ) ÷ ความลาดชันตามกฎหมาย (กฎกระทรวงกำหนดสิ่งอำนวยความสะดวกฯ ≤1:12, ยืนยันด้วย WebSearch) = **ความยาวทางลาด 14.4ม.** — ใช้ความสูงคงที่ตลอดความยาว ไม่ลดตามความชันจริง (ไม่คุ้มเวลาคิดละเอียดสำหรับรายการเล็ก)
- ผลคำนวณ: ผนังก่ออิฐเต็มแผ่น 17.28ตร.ม., คอนกรีตฐานเล็ก 0.605ม³, เหล็กหนวดกุ้ง DB9@0.20 = 72เส้น (ความยาว/เส้นยังไม่ยืนยัน ค่าน้อยไม่กระทบยอดรวม)
- **หมวดโครงสร้าง (Stage 2) ปิดครบ 100% ทุกรายการแล้ว** พร้อมสำหรับแผนเผยแพร่ให้ใช้ฟรีทาง Facebook

## 2569-08-31 (4)

`project/code/python/combine_cutting_list.py` (ใหม่), `project/new house/claude_boq.md`, `03-ai-boq-procedure.md`, `BACKLOG.md` — ปิด 2 ใน 3 รายการนอกสโคปที่เหลือ (cutting-list ข้ามหมวด, สเปครับน้ำหนัก HC)

- **รวม cutting list ข้ามหมวดทั้งโครงการ:** เขียนสคริปต์ First-Fit-Decreasing bin-packing รวมเหล็กทุกชิ้นจากฐานราก+ตอม่อ-เสา+คาน (ดึงจากตัวเลขที่ยืนยันแล้วในแต่ละสคริปต์เดิม ไม่เรียก API ซ้ำ) — ประหยัดได้ **57 เส้น (570ม., ~450กก., ~9,800-9,900บาท)** เทียบกับตัดแยกทีละหมวดแบบเดิม (458→401 เส้น)
- **สเปครับน้ำหนัก HC ค้นแล้วด้วย WebSearch:** พบมาตรฐาน/กฎกระทรวงควบคุมอาคาร 2522 (ฉบับที่ 6/2527) ที่ วสท.อิงใช้ — live load พื้นที่พักอาศัย/ห้องน้ำ ขั้นต่ำ **150 กก./ตร.ม.**, dead load พื้นสำเร็จรูป+ทับหน้า 10ซม. **240-260 กก./ตร.ม.** — สเปคในแบบ S-09 ("250กก./ตร.ม.") น่าจะหมายถึงพิกัดรับน้ำหนักของแผ่นเอง (เกิน live load ขั้นต่ำอยู่แล้ว) **แต่เป็นการตีความของไพน้อย ยังไม่ยืนยันกับเจ้าของโปรเจกต์/ผู้ผลิตแผ่นโดยตรง**
- **คานคอดินลอยใต้บันได/ทางลาด — ยังค้าง ไม่สามารถปิดเองได้** ต้องรอเจ้าของโปรเจกต์สำรวจหน้างานจริง (ตำแหน่ง/ความสูงที่ลอย) เพราะไม่มีข้อมูลนี้ในแบบเลย เป็น edge case เฉพาะจุดตามที่ระบุไว้แต่แรก

## 2569-08-31 (3)

`project/new house/claude_boq.md`, `BACKLOG.md` — ปิดเคสเหล็กพื้นยื่น (CS) จุดสุดท้ายที่ค้างไว้ — **Stage 2 (คำนวณ) หมวดโครงสร้าง ครบสมบูรณ์ทุกจุดแล้ว**

- **แก้สมมติฐานระยะยึด (anchorage) ของเหล็กบน CS ตามที่เจ้าของโปรเจกต์แก้:** ไม่ใช่ L/4 ของระเบียงเดิม (0.50ม.ที่ผมเดาไว้ก่อน) แต่คือ**ไม่เกินความลึกคาน (0.40ม.)** — เหล็กพาดเข้าคานจนเกือบชนเหล็กล่างของคาน แล้วพับ 90° ต่ออีก 10ซม. (รวมอยู่ในระยะ 0.40ม.ที่ใช้)
- ผลคำนวณสุดท้าย: เหล็ก RB9 พื้นยื่น CS = 148.7ม. = **74.2 กก.** (เหล็กบนหลัก 60.3ม. + กระจายตามแนวคาน 29.8ม. + เหล็กล่าง 58.6ม.)
- อัปเดตยอดรวมน้ำหนักเหล็กทั้งโครงการ: 2,986.5 → **3,060.7 กก.** (รวม CS แล้ว) เทียบ BOQ จริง 3,568กก. (BOQ จริงไม่มีรายการ CS เลย)
- **หมวด 1 (งานโครงสร้าง) ของ Stage 2 ปิดครบทุกรายการแล้ว** (ฐานราก, ตอม่อ-เสา, คาน, พื้น HC/S1/CS ทั้งพื้นที่+คอนกรีต+เหล็กทุกจุด) เหลือแค่รายการรอบนอกที่แยกสโคป (สเปค HC ตาม วสท., คานคอดินลอยใต้บันได, cutting-list ข้ามหมวด) ก่อนไปหมวด 2 (สถาปัตย์)

## 2569-08-31 (2)

`project/new house/claude_boq.md` — ปิดพื้นยื่น (CS), แก้เหล็กหนวดกุ้ง HC ด้วยห้องจริง, และรวมน้ำหนักเหล็กทั้งโครงการเทียบ BOQ จริงตามที่เจ้าของโปรเจกต์ขอ

- **พื้นยื่น (CS) พบระยะยื่นแล้ว = 0.50ม.** (กว้าง 10.00ม.×ยื่น0.50ม.=5.00ตร.ม., คอนกรีต 0.50ม³) — เจ้าของโปรเจกต์ชี้ว่าผมพลาดเพราะซูมครอปแยกส่วนจนไม่เห็นเส้นบอกระยะที่ขอบภาพ (บทเรียน: ต้องเผื่อ margin รอบจุดที่สนใจเสมอตอนซูม ไม่ตัดชิดจนขอบ) — ยังไม่คำนวณเหล็ก (เป็นพื้นยื่นจริง เหล็กบนต้องรับแรงหลักตลอดความยาว ไม่ใช่แค่ L/4 แบบ S1 ทั่วไป)
- **เหล็กหนวดกุ้ง HC คิดใหม่ด้วยห้องจริงจาก A-04:** เส้นรอบรูป HC รวม = 159.00ม. (แก้สูตรพื้นที่เว้าที่มุม — พื้นที่เว้าตรงมุมพอดีไม่เปลี่ยนเส้นรอบรูปเลย ต่างจากที่เคยคำนวณผิดด้วยสูตรลวกๆ) → 530 ชิ้น × 0.30ม. = 35.3 กก. — ใกล้เคียงค่าประมาณเดิมมาก (35.7) ยืนยันว่าค่านี้เสถียรไม่ไวต่อความแม่นของห้องมากนัก
- **รวมน้ำหนักเหล็กทั้งโครงการตามที่เจ้าของโปรเจกต์ขอ (เพื่อดูกำไร/ขาดทุนน้ำหนัก):** ไพน้อย 2,986.5 กก. (DB12 1,165.3 + DB16 858.4 + RB9 438.8 + RB6 524.0) เทียบ BOQ จริงรวมดิบ 3,568 กก. — **แต่ห้ามอ่านตรงๆว่าไพน้อยประหยัดกว่า 16.3%** เพราะแต่ละหมวดมีข้อผิดพลาดคนละทิศทางที่กลบกันเอง: หมวดฐานราก-ตอม่อ+เสาคาน (item2+3) ไพน้อยสูงกว่า BOQ จริงทั้งคู่ (BOQ ตีราคาต่ำไปจริง เสี่ยงขาดทุน) แต่หมวดพื้น (item4) ไพน้อยต่ำกว่ามาก (BOQ สลับไซส์เหล็กทำตัวเลขดิบสูงเกินจริง) — สรุปที่เชื่อถือได้คือ**หมวดโครงสร้างล้วนๆ (ไม่รวมพื้น) BOQ จริงตีราคาต่ำกว่าที่ต้องใช้จริงทั้ง 2 หมวด**

## 2569-08-31 (1)

`project/code/python/extract_floor_boq.py`, `project/new house/claude_boq.md`, `03-ai-boq-procedure.md`, `08-ground-truth-boq-validation.md` — ปิดเคสเหล็กพื้น S1 (RB9/RB6) ที่ค้างมาจากเมื่อวาน พบว่า BOQ จริงสลับไซส์เหล็กกัน

- **เจ้าของโปรเจกต์แก้ความเข้าใจผิดของผม:** เหล็กบนของพื้น S1 (detail "S1" ใน S-09) เป็น **RB9 เหมือนเหล็กล่าง ไม่ใช่ RB6** ตามที่ผมเข้าใจผิดมาตลอด — RB6@0.125 "คอม้าเส้นเว้นเส้น" คือคอม้า (bar chair) สั้นแค่ 20-30ซม./ชิ้น ไม่ใช่เหล็กรับแรงยาวเต็ม L/4 อย่างที่เคยคำนวณผิด
- **หลักการที่ยืนยันครบแล้ว:** เหล็กล่าง=RB9@0.15 เต็มพื้นที่ 2 ทิศทาง, เหล็กบน=RB9@0.15 เฉพาะแถบ L/4 จากขอบคาน 2 ทิศทาง (กันขอบพื้นยกตัว), พื้น S1 ที่ติดกันคนละฝั่งคาน เหล็กบนพาดผ่านต่อเนื่องเป็น L/2 ไม่ตัดแบ่งแต่ปริมาณเท่ากับคิดแยกผืน (ไม่มีในบ้านหลังนี้ ทุกผืนแยกอิสระ ใช้พับฮุกแทน)
- **คำนวณใหม่ได้: ตาข่ายหลัก RB9 = 438.8 กก., คอม้า RB6 = 24.1 กก.** — เทียบ BOQ จริง (RB9=987, RB6=438) พบ 987÷438.8=2.25 ตรงกับอัตราส่วนน้ำหนัก RB9÷RB6 (0.499÷0.222=2.25) พอดีเป๊ะ **สรุปว่า BOQ จริงสลับไซส์เหล็กกัน** ความยาวจริงตรงกับที่ไพน้อยคำนวณได้ (438.8≈438)
- **ยืนยันด้วยวิธี recheck ของเจ้าของโปรเจกต์:** เส้นผ่านศูนย์กลางเหล็กควรอยู่ที่ 8-10% ของความหนาพื้น — RB9(9มม.)÷พื้น10ซม.=9% ตรงช่วง ยืนยันว่า RB9 คือไซส์ถูกต้องของตาข่ายหลัก
- บันทึกเป็นหลักฐานที่ 4 ของ "BOQ จริงก็มีรอยเปื้อน" ใน [08-ground-truth-boq-validation.md §7.4](./08-ground-truth-boq-validation.md) (ต่อจาก code ซ้ำ, D5 นับขาด, ตอม่อสูงผิด) — ครั้งนี้เป็นประเภทใหม่: สลับป้ายไซส์วัสดุ
- **เหล็กพื้น S1 ปิดเคสสมบูรณ์แล้ว** — เหลือแค่ระยะยื่น CS และเหล็กหนวดกุ้ง HC (คิดใหม่ตามห้องจริง) ก่อนจะครบหมวดพื้นทั้งหมด

## 2569-08-30 (19)

`project/code/python/extract_floor_boq.py` (ใหม่), `project/new house/claude_boq.md`, `03-ai-boq-procedure.md` — ถอดพื้นที่พื้น HC/S1 ใหม่ทั้งหมดจาก A-04 (แบบสถาปัตย์จริง) แทน S-02 (กริดโครงสร้าง) ตามคำแนะนำเจ้าของโปรเจกต์ ตรงกับ BOQ จริงเกือบเป๊ะทุกตัวเลข

- **บทเรียนสำคัญที่สุดของรอบนี้:** พื้นที่ห้องที่ถอดจาก S-02 (กริดคาน/เสา) รอบแรกรวมยอดใกล้เคียง BOQ จริงโดยบังเอิญ แต่**แยกห้องผิดทั้งหมด** (ผนังภายในไม่ตรงกับกริดโครงสร้างเลย) — ห้องนอน 1/2 จริงกว้างเต็มถึงคอลัมน์4 (9.00ม. ไม่ใช่ 4.50ม.), ห้องน้ำเป็นห้องเล็กแทรกในห้องใหญ่ (ไม่เต็มช่วงโครงสร้าง), ห้องโถงเป็นรูปตัว L ประกอบ 4 ช่องที่กริดไม่บอกเลย, WC3 อยู่ในโซน offset เหนือแถว A ไม่ใช่แถว B-C ที่เคยเข้าใจผิด — **แก้โดยเปิด A-04 จริง ไล่เส้นประ (dimension extension line) ของแต่ละผนังไปหาตัวเลขบนเส้นบอกระยะรอบอาคาร แทนการอ้างอิงกริดคาน/เสา**
- **บันทึกกฎใหม่ใน `03-ai-boq-procedure.md`:** ห้ามใช้กริดโครงสร้าง (S-01/S-02) สมมติรูปทรง/ขนาดห้องสถาปัตย์เด็ดขาด ต้องอ่านจาก A-04 (หรือแบบสถาปัตย์ที่เกี่ยวข้อง) เสมอ
- **ถอนกฎ "ของเสียตัดแผ่น HC 5-8%" ที่เคยบันทึกผิดไว้:** ตัวเลข 6.45% ที่เคยได้มาจากพื้นที่ผิด (S-02) พอแก้เป็นพื้นที่ถูก (A-04) แล้วของเสียจริงใกล้ 0% (+0.8%) ไม่ใช่ 5-8% — หลักการของเสียตัดแผ่นยังถูกต้องทั่วไป แต่ newhouse ไม่ใช่หลักฐานยืนยันตัวเลขนี้ ต้องหาโปรเจกต์อื่นยืนยันแทน
- ผลลัพธ์สุดท้าย (สคริปต์ `extract_floor_boq.py`, โค้ด Python ล้วนจากตารางห้องที่ยืนยันแล้ว): HC สุทธิ 169.41ตร.ม.(BOQ จริง168, +0.8%), S1 สุทธิ 44.86ตร.ม.(BOQ จริง~46, -2.5%), รวม 214.27ตร.ม.(BOQ จริง214, +0.1%), คอนกรีตพื้นรวม 12.956ม³(BOQ จริง13.0, -0.3%) — ตรงกันเกือบทุกตัวเลข
- ยังค้าง: เหล็ก RB9/RB6 พื้น S1 (พยายามคำนวณเองแล้ว 2 รอบ ยังต่ำกว่า BOQ จริงมาก แม้แก้ cover และยืนยันเงื่อนไข "แยกห้องอิสระทุกห้อง" แล้ว — เข้าใจ detail S-09 ยังไม่ครบ), ระยะยื่นของ CS, เหล็กหนวดกุ้ง (คำนวณจากเส้นรอบรูปแบบ S-02 เดิม ยังไม่อัปเดตตามห้องจริง)

## 2569-08-30 (18)

`project/new house/claude_boq.md` — คำนวณเหล็กหนวดกุ้ง (RB6@0.30, ยึด topping HC กับปลอกคาน) ~35.7 กก. (ประมาณจากเส้นรอบรูปห้อง HC รวม 160.6ม. ÷ 0.30ม. = 535 ชิ้น) — เจ้าของโปรเจกต์ถามหาเหตุผลที่ยังไม่คำนวณเหล็ก RB9/RB6 พื้น S1 เอง

- **ติดขัด 2 เรื่อง (ตอบเจ้าของโปรเจกต์แล้ว):** (1) ระยะหุ้มคอนกรีตของพื้น (cover) ยังไม่ยืนยัน — กฎ "ในอากาศ 3.5ซม." ที่มีใช้กับชิ้นหนา (ฐานราก/เสา/คาน) พื้นบางแค่ 10ซม.น่าจะใช้ cover น้อยกว่านั้น (2) S-09 แสดงเหล็กบน RB9 ยื่น L/4 จาก "ทั้ง 2 ฝั่ง" ของช่วง L,L1 ที่ติดกันคนละคาน — เป็นพื้นต่อเนื่องหลายช่วง (2-way continuous slab) ไม่ใช่แยกอิสระทีละห้องแบบคาน ยังไม่รู้ว่าห้อง S1 ไหนใช้เหล็กร่วมกันตรงคานร่วมบ้าง — รอเจ้าของโปรเจกต์ให้กฎง่ายๆ (แบบที่เคยให้ L/4 ของคาน) หรือยืนยันว่าคิดแยกห้องอิสระไปเลย

## 2569-08-30 (17)

`03-ai-boq-procedure.md`, `project/new house/claude_boq.md` — ปิดเคสส่วนต่างพื้นที่ HC 18 ตร.ม. — พบเป็นของเสียมาตรฐานจากการตัดแผ่นพื้นสำเร็จรูป ไม่ใช่พื้นที่ห้องที่ตกหล่น

- **เจ้าของโปรเจกต์ทายว่าเป็นของเสียจากการตัดแผ่นพื้นเข้ามุมเสา ~5-8%** — ตรวจสอบ: ถ้ารวมแถบเฉลียงนอกกล่อง S1 (offset zone คอลัมน์1-2, 5-6) เป็นพื้นที่ HC สุทธิด้วย (8.17 ตร.ม.) ได้พื้นที่สุทธิรวม 157.82 ตร.ม. เทียบกับพื้นที่สั่งผลิตจริงตาม BOQ (168 ตร.ม.) = ของเสีย **6.45%** อยู่ในช่วง 5-8% พอดี — คอนกรีต topping+S1 รวมก็ตรงกับ BOQ จริงเป๊ะ (13.0 ม³) ยืนยันการจำแนกโซนถูกต้องทั้งหมด
- **บันทึกเป็นกฎมาตรฐานใหม่ใน `03-ai-boq-procedure.md` หมวด 1:** พื้นที่สั่งผลิตแผ่นพื้นสำเร็จรูป (HC) = พื้นที่สุทธิ ×1.05-1.08 (คนละสาเหตุกับของเสียเหล็ก bar cutting list — นี่คือของเสียพื้นที่สั่งผลิตจากการตัดเข้ามุมเสา/ขอบผนัง)
- อัปเดต `claude_boq.md` หมวด 4 (พื้น): พื้นที่ HC สุทธิ=157.82 ตร.ม., สั่งผลิตจริง=168 ตร.ม. (ตรง BOQ จริงเป๊ะ) — เหลือ: พื้นยื่น CS (ระยะยื่นยังไม่รู้), เหล็ก RB9/RB6 พื้น S1 (ยังยืมตัวเลข BOQ จริงอยู่)

## 2569-08-30 (16)

`project/new house/claude_boq.md` — เริ่มเฟส 2/4 (พื้น): ซูม S-02 ทีละแถว (A-B, B-C, C-D, D-E) จำแนกห้องเป็น HC (พื้นสำเร็จรูป) หรือ S1 (เทในที่) ตามสัญลักษณ์ คำนวณพื้นที่จากกริดที่ยืนยันแล้ว

- **พื้นที่ S1 (ห้องน้ำ row B-C ทั้งแถว + กล่องทางเข้า offset zone + ห้องมุม D-E คอลัมน์1) = 46.19 ตร.ม. ตรงกับ BOQ จริง (~46 ตร.ม., ย้อนคำนวณจากคอนกรีต) เกือบเป๊ะ** — คอนกรีต topping(168×0.05=8.4)+S1(46×0.10=4.6)=13.0 ม³ **ตรงกับ BOQ จริงเป๊ะ (13.0 ม³)** ยืนยันวิธีจำแนกโซน S1/HC ถูกต้อง
- พื้นที่ HC ที่ถอดได้จากห้องหลัก (149.65 ตร.ม.) ต่ำกว่า BOQ จริง (168 ตร.ม.) ~18 ตร.ม. — คาดว่าเป็นแถบเฉลียง/ช่องเปิดโซน offset (หน้าบ้าน คอลัมน์1-2 กับ 5-6) ที่ยังไม่ชัดว่านับเป็น HC หรือพื้นภายนอกคนละระบบ — ใช้ค่า BOQ จริง (168) แทนไปก่อน ไม่เดา
- ยังไม่ได้คำนวณเหล็ก RB9/RB6 ของพื้น S1 เอง (ยืมตัวเลข BOQ จริงมาใช้ก่อน เพราะต้องรู้ขนาดห้องจริงแต่ละห้อง ไม่ใช่แค่พื้นที่รวม) และยังไม่รู้ระยะยื่นของ CS (พื้นยื่น)

## 2569-08-30 (15)

`08-ground-truth-boq-validation.md`, `project/new house/claude_boq.md` — ปิดเคสส่วนต่าง 23%/16.1% ระหว่างไพน้อยกับ BOQ จริง (หมวดฐานราก-ตอม่อ) พร้อมพบข้อคิดทางธุรกิจสำคัญ

- **สาเหตุยืนยันแล้ว:** ไฟล์ `BOQ - new house.xls` ใช้ความสูงตอม่อ **0.8ม.** ตอนตีราคาจริง แต่หน้างานจริงที่สร้างคือ **1.2ม.** (เท่ากับค่าที่เจ้าของโปรเจกต์ให้ไพน้อยใช้ในเซสชันนี้) — **ตัวเลขของไพน้อยแม่นกว่าไฟล์ BOQ เดิม ไม่ใช่ไพน้อยผิด**
- **นัยธุรกิจสำคัญ:** ถ้าจริงตามนี้ แปลว่าโครงการนี้ตีราคาให้ลูกค้าต่ำกว่าปริมาณจริงที่ต้องใช้ ~20% ในหมวดฐานราก-ตอม่อ — เสี่ยงขาดทุนจริงโดยไม่มีใครรู้ตอนเสนอราคา เป็นหลักฐานที่ 3 ของ "BOQ เองก็มีรอยเปื้อน" (ต่อจาก code ซ้ำ และ D5 นับขาดที่เคยพบมาก่อน) — บันทึกเต็มไว้ที่ [08-ground-truth-boq-validation.md §7.3](./08-ground-truth-boq-validation.md)
- **สรุปคุณค่างานนี้เป็น "3 เด้ง" (คำเจ้าของโปรเจกต์เอง):** (1) ถอดปริมาณ (2) เทียบกับ BOQ เดิมถ้ามี — จับส่วนต่างก่อนขาดทุนจริง (3) บังคับยืนยันตัวเลขที่มักตกหล่นตอนตีราคาเร็ว (ความลึก/ความสูง/ระยะ) — บันทึกเป็น framing หลักของคุณค่าเครื่องมือนี้แล้ว
- ปมเรื่องเหล็ก DB12 vs DB16 ที่เคยสงสัยว่า BOQ จริงไม่มี DB16 เลย: เจ้าของโปรเจกต์ยืนยันว่า **ไฟล์ BOQ พิมพ์ผิด/รวมไซส์ผิดจริง** ไม่ใช่หน้างานใช้ไซส์อื่น — เทียบด้วยน้ำหนักรวมทุกไซส์แทนได้ผลใกล้เคียงดี (+16.1% ที่ตอม่อ1.2ม., +14.0% ถ้าลอง0.8ม.)

## 2569-08-30 (14)

`project/code/python/compute_formwork_and_compare.py` (ใหม่), `project/new house/claude_boq.md` — เปรียบเทียบผลคำนวณโครงสร้าง (ฐานราก+ตอม่อ+เสา+คาน) กับ BOQ จริง (`boq/BOQ - new house.xls`) + คำนวณไม้แบบเพิ่ม

- **แก้บั๊กที่พบระหว่างทาง:** ตาราง "1. ฐานราก" ใน `claude_boq.md` แสดงผิดว่า F2/F3 หนา (T) 0.20ม.เท่ากันหมด ทั้งที่ตัวเลขปริมาตรที่คำนวณจริง (2.75, 4.056 ม³) ใช้ T=0.25 และ 0.30 ตามลำดับ (อ่านถูกจาก S-05 ตั้งแต่แรก แค่ตารางแสดงผลพิมพ์ผิด) — แก้ตารางให้ตรงกับตัวเลขจริงแล้ว
- **คอนกรีตเสา+คาน (above floor) ตรงกับ BOQ จริงมาก:** 13.893 ม³ (ไพน้อย) vs 14.0 ม³ (BOQ จริง) ต่างแค่ 0.8% — ยืนยันความแม่นของงานเสา+คานที่ทำไปทั้งหมด
- **ไม้แบบ (คำนวณใหม่ด้วยสูตรมาตรฐาน) ก็ใกล้เคียงดี:** ฐานราก+ตอม่อ 47.96 vs 50.0 ตร.ม.(-4.1%), เสา+คาน 186.84 vs 206.0 ตร.ม.(-9.3%)
- **⚠️ พบจุดต้องตรวจสอบ 2 จุด:** (1) คอนกรีตฐานราก+ตอม่อสูงกว่า BOQ จริง 23% (8.611 vs 7.0 ม³) — ยังไม่ทราบสาเหตุแน่ชัด (2) **เหล็กเสาคาน BOQ จริงมีแต่ DB12 (1,259กก.) ไม่มี DB16 เลย แต่ S-08 (ที่ผมเปิดดูภาพเองโดยตรง ไม่ใช่ AI) ระบุ B5/B6 เป็น DB16 ชัดเจน** ลองจับคู่ตัวเลขทุกแบบ (รวม/ไม่รวมส่วนตอม่อ) ก็ไม่ตรงกับ 1,259กก.เลย — ไม่กล้าสรุปเอง (อาจเป็นไปได้ทั้งหน้างานจริงใช้ DB12 แทน, ผมอ่านแบบผิด, หรือ BOQ จริงพิมพ์ผิด) รายงานให้เจ้าของโปรเจกต์ตรวจสอบแล้ว ยังไม่ได้คำตอบ

## 2569-08-30 (13)

`project/new house/claude_boq.md`, `BACKLOG.md` — แก้ครบ 7 segment คานที่ค้างไว้จากรอบก่อน (SW cluster) จนได้ BOQ คาน B1-B6 ครบ 39/39 segment

- **วิธีแก้:** ซูม S-02 เฉพาะจุด (แถว C-D-E, คอลัมน์ 1-4) ที่ 500dpi ให้เห็นทั้งคานและแกนกริด(วงกลม 1/2/3/4)ในภาพเดียวกัน เทียบตำแหน่งเส้นกับวงกลมกริดโดยตรงแทนการอ่านจากภาพรวมที่โมเดลอ่านไม่ชัด — พบว่า 2 จุดที่เคยอ่าน/สันนิษฐานว่าเป็น "B3" จริงๆคือ "B5" (แถว C ช่วง col1-2, col2-4) และ 1 รายการที่เคย flag เป็น "แนวตั้ง" จริงๆคือคานแนวนอนสั้น
- **กติกาที่ใช้ปิดช่องว่าง:** ความยาวคานแนวตั้ง (เสาเข็มขัด/ทับหลังรอบห้อง) = ความยาวช่วงแถว (row span) เสมอ ไม่ต้องรู้ตำแหน่งคอลัมน์แม่นยำ เพราะแนวตั้งวิ่งเต็มความสูงบ่อนั้นเสมอ — หลักการนี้ปลดล็อกจุดที่ตำแหน่ง x อ่านยากแต่ความสูงแถวรู้แน่นอนอยู่แล้ว
- ผลคำนวณครบ (39 segment, โค้ด Python ล้วน): คอนกรีตคาน 11.157ม³(+waste3%), เหล็กยืน DB12/DB16 112เส้น/1,120ม.(ของเสีย17.9%), เหล็กปลอก RB6 164เส้น/1,640ม.(ของเสีย9.0%) — เหลือ 1 รายการความเชื่อมั่นระดับกลาง (คาน B2 สั้น 2.00ม. ห้องมุมซ้ายล่าง อ้างอิงตำแหน่งจาก bottom tick ไม่ใช่ตัวเลขพิมพ์กำกับตรงจุด) ผลกระทบต่อยอดรวมน้อยมาก ไม่ตามต่อ
- **⚠️ เจ้าของโปรเจกต์ตรวจพบจุดที่ผมอ่านผิดอีก 1 จุด** (แถว D-E คอลัมน์1): เข้าใจผิดว่าเส้นแบ่งระบบพื้น "2.80" เป็นคานตัดแบ่งความสูงแนวตั้ง ทั้งที่จริงแนวตั้ง (B3,B2,B3) วิ่งเต็มช่วง D-E 4.50ม.ทั้งหมด ส่วนคานสั้น 2.00ม.ที่จุดนั้นคือ **B1** ไม่ใช่ B2 ที่เดาไว้ — แก้แล้วตามที่ยืนยัน รวม 38 segment (ไม่ใช่ 39): คอนกรีตคาน 11.388ม³(+waste3%), เหล็กยืน 113เส้น/1,130ม.(ของเสีย17.5%), เหล็กปลอก 167เส้น/1,670ม.(ของเสีย9.4%)
- **บทเรียนสำคัญจากเจ้าของโปรเจกต์:** เวลาคุยตำแหน่งคาน/เสาบนกริด ต้องบอกตำแหน่งเป็น "คอลัมน์ X, แถว Y-Z" แบบวิศวกรคุยกันเสมอ ไม่ใช่คำอธิบายกว้างๆ — บันทึกเป็น feedback memory แล้ว ใช้กับงานถอดโครงสร้างทุกจุดต่อไป
- อัปเดตยอดรวมคอนกรีตโครงสร้างสะสม (ฐานราก+ตอม่อ+เสา+คาน): 22.504 ม³ (+lean 2.309 ม³), เหล็ก DB12/16 รวม 232 เส้น (2,320ม.), เหล็ก RB6 รวม 226 เส้น (2,260ม.) — ทั้งหมดอยู่ใน `claude_boq.md` แล้ว

## 2569-08-30 (12)

`project/code/python/extract_beam_boq.py` (ใหม่), `project/new house/claude_boq.md` (เปลี่ยนชื่อจาก `structural_boq.md` — รวมทุกหมวดในไฟล์เดียว ไม่ใช่แค่โครงสร้าง), `03-ai-boq-procedure.md`, `BACKLOG.md`, `project/new house/foundation_data.md` — เริ่มถอดปริมาณคาน B1-B6 (newhouse), พบว่าพื้นบ้านมี 3 ระบบปนกัน

- **แก้ page-mapping เก่าใน `foundation_data.md`:** Stage 0 เคยบันทึกหน้า 19-28 เป็น "illegible" และหน้า 19="S-08" ผิด — เปิดดู title block จริงทุกหน้า (contact sheet) แก้เป็น 19=S-06, 20=S-07, 21=S-08, 22=S-09, 23-25=E-01/02/03, 26-28=SN-01/02/03 ตรงแล้ว — พบว่า S-02(หน้า15)="แปลนเสา,คาน,พื้น" และ S-08(หน้า21)="แบบขยายโครงสร้างคาน" ตรงกับที่ BACKLOG.md เดาไว้พอดี
- **เจ้าของโปรเจกต์ยืนยันกฎใหม่ (หมวด 1):** เหล็ก "ADD." ในคาน B3-B5 ยื่นยาว = L/4 ของ**ความยาวคานเส้นนั้นเอง** (ไม่ใช่ค่าคงที่) — หลักการเดียวกับที่ใช้ในแบบขยายเหล็กพื้นบนคาน (S-09 "S1" detail)
- **พบพื้นบ้านมี 3 ระบบปนกัน ไม่ใช่ระบบเดียว:** (1) "HC" = พื้นสำเร็จรูป (precast plank, Hollow Core) พื้นที่ส่วนใหญ่ — **ยังไม่มีสเปครับน้ำหนักในแบบเลย** (แบบชุดนี้ copy จากโปรเจกต์อื่น) เจ้าของโปรเจกต์เสนอให้ใช้มาตรฐาน วสท. (dead load~300, live load~100-200 กก./ตร.ม.) แต่ยังไม่ตัดสินใจว่าใครค้นข้อมูลนี้ (2) "S1" = พื้นเทในที่ (ไม่ใช่พื้นสำเร็จรูปอย่างที่เข้าใจผิดตอนแรก) (3) "CS" = พื้นยื่น (cantilever) เทในที่ เทพร้อม S1
- **ยืนยันจากเจ้าของโปรเจกต์:** มีจุดคานคอดินลอยสูงจากพื้นดินเดิมจริง 1 จุด (ใต้บันได/ทางลาดขึ้นบ้าน) — ตอนก่อสร้างจริงไม่มีแบบขยายจุดนี้เลย ต้องแยกสำรวจ/ถอดเป็นรายการของตัวเอง ไม่ใช้ตัวเลขจากแบบขยายทั่วไปใน S-09 (ส่วนนั้นเป็นเนื้อหา copy-paste ไม่ตรงกับ site จริง)
- **⚠️ อ่าน S-02 ยากกว่าฐานราก/ตอม่อมาก:** ไม่ใช่กริดจุดซ้ำๆ แต่เป็นเส้นคานความยาวต่างกันตามช่วงจริง (บาง bay ถูกแบ่งย่อยด้วย sub-dimension ที่ไม่ตรงกับช่วงกริดหลัก) — โมเดลอ่านได้ 35 segment แต่ยอมรับเองว่า 7 segment (กระจุกห้องมุม SW) อ่านความยาวไม่ชัด **ไม่ยอมเดา** (พฤติกรรมที่ถูกต้องตามกฎ) — คำนวณ BOQ ด้วย 28 segment ที่ยืนยันแล้วก่อน: คอนกรีต 7.432ม³(+waste3%), เหล็กยืน 80เส้น/800ม.(ของเสีย 22.2%), เหล็กปลอก 111เส้น/1,110ม.(ของเสีย 9.7%) — ยังไม่ใช่ยอดเต็ม
- แก้บั๊ก `call_single()` เพิ่ม (extract_foundation_data.py): max_tokens สูง (>10000) ทำให้ SDK error "streaming required for >10min operations" — เปลี่ยนเป็นเรียกผ่าน `client.messages.stream()` แทน `create()` ตรงๆ แก้ได้ถาวร ใช้ได้กับทุกสคริปต์ที่ import call_single
- เปลี่ยนชื่อ `structural_boq.md` → `claude_boq.md` ตามคำขอเจ้าของโปรเจกต์ ปรับโครงสร้างให้ครอบคลุมทุก 7 หมวด (ไม่ใช่แค่โครงสร้าง) — เป็นไฟล์ผลลัพธ์ BOQ ที่ยืนยันแล้วไฟล์เดียวของทั้งโปรเจกต์ต่อไป

## 2569-08-30 (11)

`project/code/python/extract_pier_column_boq.py` (ใหม่), `03-ai-boq-procedure.md`, `BACKLOG.md` — ถอดปริมาณคอนกรีต+เหล็กตอม่อ-เสา (C1/Cx) newhouse สำเร็จ ครบทั้ง 23 จุด

- **บันทึกกฎเหล็กเสริมตอม่อ-เสาบ้านชั้นเดียวลง `03-ai-boq-procedure.md` หมวด 1:** เหล็กยืนเป็นเส้นเดียวต่อเนื่อง ไม่มีระยะทาบ ตัดจุดเดียวที่ปลายบนสุด, พับขอ 90° ที่ฐานราก = ครึ่งหนึ่งของด้านยาวสุดของฐานรากนั้น (แยกตามชนิด ไม่ใช่ค่าเดียวทั้งโปรเจกต์), และหลักการหาความสูงเสาคอนกรีตทั่วไป (ฝ้า+10ซม. เป็นค่าต่ำสุด ต้องอยู่ระหว่างฝ้ากับอะเส ปัดขึ้นเผื่อคลาดเคลื่อนหน้างานเป็นเลขกลม) — newhouse: ฝ้า+10ซม.=3.10ม. (ต่ำสุด) ปัดเป็น **3.20 ม.** (เผื่อ 10ซม.)
- **⚠️ พบ AI อ่าน S-01 ผิดซ้ำอีกครั้ง** (รอบนี้ 28 จุด vs ค่าที่ตรวจมือแล้ว 23 จุด, นับแถว D/E เกินไป) — เปิดภาพ 350dpi นับเองยืนยัน 23 จุดตรงกับรอบก่อนเป๊ะ (F1=4,F2=11,F3=8) — ย้ำบทเรียนเดิม: หน้ากริดหนาแน่นห้ามเชื่อผลนับ AI ครั้งเดียว ต้องมีการตรวจสอบซ้ำเสมอ
- ผลคำนวณ (นับมือยืนยันแล้ว + อ่าน S-05/S-06 ผ่าน Sonnet 5 + คำนวณด้วยโค้ด Python ล้วน): คอนกรีตตอม่อ+เสา รวม 3.536 ม³ (+waste3%=3.642 ม³), เหล็กยืนเต็มความสูง (C1, พับ+ตอม่อ1.2+เสา3.2ม.) 81 เส้น/810ม., เหล็กยืนเฉพาะตอม่อ (Cx — ด้านบนพื้นเป็นเหล็กกล่อง 4"x4"x3.2mm ไม่ใช่คอนกรีต ไม่นับเหล็กยืนต่อขึ้นไป) 3 เส้น/30ม., เหล็กปลอก RB6 59 เส้น/590ม. — ต้นทุน API ~4 บาท
- ค้างไว้ตรวจสอบ: ระยะเหล็กปลอกที่ S-06 อ่านได้ (@0.15ม.) ต่างจากที่เคยเห็นใน S-05 (@0.10ม.) — คนละหน้าคนละบริบท ยังไม่ได้เปิดเช็คว่าใช้ค่าไหนตรงส่วนไหนของเสา

## 2569-08-30 (10)

`project/code/python/extract_footing_boq.py` (ใหม่) — **เขียนสคริปต์จริงตัวแรกของ Stage 2 (คำนวณ) สำเร็จ** ถอดปริมาณคอนกรีต+เหล็กฐานรากอัตโนมัติ อ่าน S-01+S-05 ด้วย Sonnet 5 จริง คำนวณด้วยโค้ด Python ล้วน (ไม่ให้ AI คิดเลขเอง) — **ผลลัพธ์ตรงกับที่ตรวจมือ 100% ทุกตัวเลข** หลังแก้บั๊ก max_tokens (Sonnet 5 ใช้ adaptive thinking กิน token จนหมดก่อนตอบถ้าตั้งน้อยไป ต้อง ≥10000 สำหรับงานนับทั้งหน้า) ต้นทุนจริง $0.09 (~3.28 บาท)

- บันทึกกฎคอนกรีตหยาบ (7.5ซม.คงที่, พื้นที่×1.15), waste คอนกรีตโครงสร้าง 3%, มาตรฐานระยะหุ้ม (ในน้ำ7ซม./ในดิน5ซม./ในอากาศ3.5ซม.) ลง `03-ai-boq-procedure.md` แล้ว — ฐานรากใช้ "ในดิน" = 5ซม.
- เปรียบเทียบวิธีคำนวณเหล็ก 2 แบบ (น้ำหนัก vs bar cutting list จากเหล็ก 10ม.) ด้วยฐานราก F1-F3 ทั้งหมด (23 จุด) — เจ้าของโปรเจกต์ยืนยันว่า cutting list ตรงกับการสั่งของจริง (สั่งเป็นเส้น) ใช้เป็นวิธีมาตรฐานต่อไป — พบว่าของเสียจริงต่างกันมากตามชนิดฐานราก (F1=30.7%, F2=1.0%, F3=4.0%) ไม่ใช่ % คงที่แบบที่เคยสมมติ
- นี่คือครั้งแรกที่ไพน้อย (โค้ดจริง ไม่ใช่ Claude นั่งทำมือในแชท) ถอดปริมาณงานโครงสร้างได้เองสำเร็จ — พิสูจน์แล้วว่างาน "นับรหัสบนกริด + อ่านตาราง schedule" ทำเป็นสคริปต์อัตโนมัติได้จริง ไม่ต่างจากประตู/หน้าต่างที่เคยพิสูจน์ไปก่อนหน้านี้

## 2569-08-30 (9)

`04-category-schema.md`, `03-ai-boq-procedure.md` — จัดหมวดงานให้ครบ 7 หมวดมาตรฐานตามที่เจ้าของโปรเจกต์ระบุ (โครงสร้าง/สถาปัตย์/ไฟฟ้า/สุขาภิบาล/ปรับอากาศ/เบ็ดเตล็ด/เตรียมการฯ) และแก้จุดที่สุขภัณฑ์เคยซ้ำอยู่ทั้งหมวด 2 (สถาปัตย์) และหมวด 3 (สุขาภิบาล)

- ยืนยันจากเจ้าของโปรเจกต์: **สุขภัณฑ์เป็นงานสถาปัตย์ตามระเบียบราชการ** — ย้าย keyword สุขภัณฑ์/อ่างล้างหน้า/โถส้วม/โถปัสสาวะ ออกจากหมวด 3 ทั้งหมด เหลือแค่หมวด 2.6 หมวด 3 (สุขาภิบาล) ตอนนี้เหลือเฉพาะระบบท่อ/บ่อพัก/ถังบำบัด (โครงสร้างพื้นฐาน ไม่ใช่ตัวอุปกรณ์)
- **แก้ไขเพิ่มเติมทันที:** floor drain (FD) เจ้าของโปรเจกต์ยืนยันว่าอยู่หมวดสถาปัตย์ด้วยเหมือนสุขภัณฑ์อื่นๆ — ย้าย FD เข้าหมวด 2.6 ทั้งหมด ยุบหมวดย่อย 3.4 ทิ้ง (เนื้อหาว่างเปล่าหลังย้าย FD ออก) เหลือ 3.1/3.2/3.3/3.5 (เว้นเลข 3.4 ไว้พร้อมหมายเหตุอธิบาย ไม่ renumber ทับ)
- **จัดเรียง `03-ai-boq-procedure.md` ใหม่ทั้งหมดตาม 7 หมวด** — ย้ายกฎผนัง/ฝ้า/ทาสีเข้าหมวด 2, ย้ายกฎอ่านแบบโครงสร้าง+ฐานราก-ตอม่อ-ดินเข้าหมวด 1, เพิ่มหัวข้อว่างพร้อมหมายเหตุสำหรับหมวด 3/5/6/7 (ยังไม่มีเนื้อหา) ให้มีที่เติมทีหลังชัดเจน ไม่ต้องหาที่ใหม่ทุกครั้ง
- เพิ่มหมวด 7 "งานเบ็ดเตล็ด (Miscellaneous)" ใหม่ — **ยังไม่มี keyword/cad_search_layers เพราะไม่มีหลักฐานจากไฟล์จริงรองรับ** (ต่างจากหมวด 1-6 ที่ผ่านการเทียบกับ newhouse 2569.dxf/BOQ จริงแล้ว) รอตัวอย่างจริงจากโปรเจกต์ถัดไปก่อนเติม keyword
- **ถอดปริมาณคอนกรีต+เหล็กฐานรากจริงด้วยมือ (F1-F3 newhouse)** — นับจุดฐานรากจาก S-01 (F1=4, F2=11, F3=8, รวม 23 จุด, นับ 2 รอบตรงกัน), ดึงขนาด/เหล็กจากตาราง S-05 → คอนกรีตฐานราก 7.256 ม³ (+waste 3% = 7.474 ม³) + คอนกรีตหยาบ 7.5ซม.×พื้นที่1.15 = 2.309 ม³ (เจ้าของโปรเจกต์ยืนยันตัวเลขทั้งหมด)
- **เปรียบเทียบวิธีคำนวณเหล็ก 2 แบบด้วย F1 เป็นตัวอย่าง:** วิธีน้ำหนัก (BOQ ทั่วไป, waste 5%) vs วิธี Bar Cutting List (ตัดจากเหล็ก 10ม.จริง) — **เจ้าของโปรเจกต์ยืนยันว่าวิธี cutting list ตรงกับการสั่งของจริงมาก** (สั่งเป็นเส้น ไม่ใช่กิโล) พบว่าของเสียจริงจากการตัด F1 อย่างเดียวสูงถึง ~31% (มากกว่า waste 5% ที่เคยสมมติไว้มาก) เพราะไม่ได้รวมเศษกับเหล็กหมวดอื่น — บันทึกเป็นวิธีมาตรฐานใน `03-ai-boq-procedure.md` แล้ว พร้อมข้อควรระวังว่าต้องรวมทุกหมวดเหล็กทั้งโครงการก่อนตัดจริงถึงจะลดของเสียได้ — ⚠️ ยังไม่ยืนยันระยะหุ้มคอนกรีต (คอมมิตไว้ที่ 5ซม. สมมติ) ต้องเช็คซ้ำ

## 2569-08-29 (8)

`project/code/python/extract_foundation_data.py` (ใหม่), `project/new house/foundation_data.md` (ใหม่), `12-extraction-quality-log.md` (ใหม่), `13-thai-drafting-symbol-standards.md` (ใหม่), `03-ai-boq-procedure.md`, `BACKLOG.md`, `README.md` — สร้าง Stage 0 (ข้อมูลพื้นฐานโครงการ) เสร็จสมบูรณ์ครั้งแรก + กลับไปคุยเรื่องจัดอันดับ pain point ต่อด้วยตัวอย่างจริง (S-01/S-02/S-05)

- Stage 0 อ่านครบ 5 หมวด (โครงการ/ที่ดิน/สารบัญแบบ/สัญลักษณ์แบบ/สเปค) ด้วย Sonnet 5 (Haiku หลอนหนักเกินไปสำหรับตารางข้อความหนาแน่น แม้ความละเอียดสูง) ผ่านการตรวจสอบมนุษย์ทีละจุดจนครบ — ใช้เวลาหลายรอบ (ดู `12-extraction-quality-log.md` บันทึกครบ 12 รอบ) เจอบั๊กจริงหลายจุด (คำนวณขนาด base64 ผิด, ครอป title block ผิดตำแหน่ง, field-swap ซ้ำๆในตาราง legend, AI เสนอกรอบภาพเองแม่นน้อยมาก) และ**เจอ verification-gap ของ Claude เอง** (ตรวจแค่ภาพรวมย่อส่วนแทนที่จะเปิดไฟล์จริงทีละไฟล์ — บทเรียนสำคัญที่สุดของรอบนี้)
- สร้างระบบครอปรูปสัญลักษณ์อ้างอิง (`notation_symbol_boxes`) ฝังในไฟล์ md คู่กับ checklist "ถูกต้อง/แก้ไข: ___" (พิมพ์ "ตัดออก" = ไม่ใช้) แทนพึ่งคำบรรยายข้อความอย่างเดียว — เจ้าของโปรเจกต์แก้ไขจริงหลายจุด (ตัดสัญลักษณ์ ป1/น1 ที่ไม่ตรงกับการใช้งานจริง, แก้แล้วก็กลับคำแก้ไขเองเรื่อง "เหล็ก"↔"ก่ออิฐมอญ" — บันทึกไว้โปร่งใสทั้งสองครั้ง)
- ค้นคว้ามาตรฐานสัญลักษณ์เขียนแบบไทย (อาษา 2554) จริง — สรุปว่ามาตรฐานกำหนดแค่หลักการ/ตำแหน่ง ไม่ได้บังคับรูปทรงสัญลักษณ์ตายตัว ยืนยันว่า Office Standard ของแต่ละสำนักงาน (เช่น RPS) คือของจริงที่ต้องอ่านทุกโปรเจกต์ ไม่ใช่เดาจากมาตรฐานกลาง
- กลับไปคุยต่อเรื่องจัดอันดับ pain point ที่ยากสุด (ค้างมาตั้งแต่ก่อน Stage 0) — เปิดดู S-01→S-02→S-05 จริงเป็นตัวอย่าง พบว่า**การอ่านรหัส+ตาราง schedule (เช่น Footing No. F1/F2/F3 ใน S-05) ไพน้อยทำได้แม่นอยู่แล้ว ไม่ใช่จุดยาก** — จุดยากจริงคือ**ข้อมูลนอกแบบที่ไม่มีใน PDF เลย** เช่น ระดับดินเดิม/ระดับถนน (ต้องสำรวจหน้างานเท่านั้น) ซึ่งกระทบความสูงตอม่อ+ปริมาตรขุด/ถมทั้งหมด — บันทึกกฎ (ระยะเผื่อทำงานฐานราก, %เผื่อดินถม, สูตรความสูงตอม่อ) ไว้ใน `03-ai-boq-procedure.md` พร้อมหลักการ**ไพน้อยต้องหยุดถามคนถ้าไม่มีข้อมูลระดับดิน ห้ามกำหนด default เอง**
- ตรวจสอบมาตรฐาน มยผ. 1105-64 (กรมโยธาธิการ) จริง พบว่าตัวเลข "50-60ซม./30%" ที่ใช้ในวงการประเมินราคา**ไม่ใช่ตัวเลขเดียวกับที่มาตรฐานวิศวกรรมฉบับนี้ระบุ** (คนละหน่วย/คนละบริบท) — บันทึกแยกไว้ชัดเจนกันสับสน

## 2569-08-26 (7)

`03-ai-boq-procedure.md`, `08-ground-truth-boq-validation.md`, `BACKLOG.md`, `CLAUDE.md`, `project/code/python/pdf_vision_reader.py` (ใหม่), `project/code/python/pdf_vision_batch_reader.py` (ใหม่) — รัน pipeline จริงผ่าน Claude API (Haiku 4.5 + Batch) ครั้งแรก: ประตู/หน้าต่าง/สุขภัณฑ์/สวิตช์แม่นยำสูง, ไฟฟ้า/ผนังยังไม่สำเร็จ

- ได้ API key จากเจ้าของโปรเจกต์ ยืนยันหลักการก่อนใช้เงินจริงทุกครั้ง (procedure ใน `03-ai-boq-procedure.md` §Pipeline คู่ขนาน) แล้วรันจริง: ประตู D1-D4 ตรง BOQ (D5 ยืนยันแล้วว่า BOQ เองผิด), หน้าต่าง W1/W3 ตรงเป๊ะ, สุขภัณฑ์ 9/10 ตรง 100% หลังแก้บั๊กครอปเผลอกินขอบห้องข้างเคียง, สวิตช์ไฟ 1/2 ทาง ตรง BOQ เป๊ะทั้งคู่ — ต้นทุนรวมทุก API call วันนี้ไม่ถึง 5 บาท
- ยืนยันซ้ำบทเรียนสำคัญ: สัญลักษณ์ที่เบียดกันแน่น (เช่น FD ในห้องน้ำ) ต้องซูมครอปเฉพาะจุดแยกอีกชั้น ไม่พอแค่ตัด tile ระดับห้อง — บันทึกเป็นกฎถาวรในขั้น D.1 ของ procedure
- ทดสอบไฟฟ้า (สวิตช์/โคมไฟ/ปลั๊ก) และผนัง (พื้นที่+เสาเอ็น+คานทับหลัง) — **สวิตช์แม่น แต่โคมไฟเพดาน (สัญลักษณ์ซ้ำไม่มีรหัสกำกับ) และผนัง (อ่านตัวเลขขนาดผิดจุด, ไม่มีตำแหน่งเสาเอ็นในแบบเลย) ยังไม่สำเร็จ** แม้ลองซูมเจาะจุดซ้ำก็ยังได้ผลไม่นิ่ง (14 แล้ว 45 คนละรอบ) — สรุปสาเหตุแท้จริง: **รายการที่มีรหัส/ตัวอักษรกำกับแต่ละจุด (ประตู/หน้าต่าง/สุขภัณฑ์/สวิตช์) ทำได้แม่นเพราะเป็นงาน "อ่านตัวหนังสือ" ส่วนสัญลักษณ์ซ้ำไม่มีรหัส (โคมไฟ/เสาเอ็น) เป็นงาน "นับรูปทรงเหมือนกันเยอะๆ" ที่ยังไม่แม่น**
- ทดสอบ local AI (Ollama qwen3.8:27b, qwen2.5vl:7b) เทียบกับ cloud (Haiku) — local ล้มเหลวทั้งคู่ (ช้าเกิน/hallucinate ตอบซ้ำแบบเดิมไม่สนใจภาพจริง) ไม่ต้องลองซ้ำในอนาคต
- เจ้าของโปรเจกต์ส่งไฟล์ตัวอย่าง "AI Agent Construction Masterclass" มาเทียบวิธี — พบว่าแนวทางในไฟล์ (แปลง PDF เป็นโมเดล 3D ที่ติด tag ก่อน แล้วถอด BOQ จากโมเดล) **ไม่ได้แก้ปัญหาที่ต้นตอ** เพราะ AI ยังต้องอ่านภาพให้ถูกก่อนถึงจะวาง 3D object ถูก ความผิดพลาดแค่ถูกย้ายไปซ่อนในโมเดลแทน — เจ้าของโปรเจกต์ชี้ประเด็นนี้เองถูกต้อง เสนอทางเลือกไว้ 4 แนวสำหรับคุยต่อ (self-consistency check, ยอมรับ 2 เกรดความแม่นยำ, YOLO ระยะยาว, ขอให้ลูกค้าใส่รหัสกำกับตั้งแต่ต้น) ยังไม่ตัดสินใจ
- เพิ่มกฎถาวรใน `CLAUDE.md`: ทุกตารางเทียบปริมาณต้องระบุแหล่งที่มาชัดเจน (จากแบบ-AI / จากแบบ-ไพน้อย / BOQ) ห้ามเรียกผลจาก Claude ว่า "ไพน้อย"
- อัปเดต `BACKLOG.md` สรุปสถานะและงานต่อทั้งหมด — พรุ่งนี้เริ่มต่อจากตรงนี้ได้เลย

## 2569-08-26 (6)

`01-market-competitors.md`, `02-business-model-finance.md`, `11-founding-member-pricing.md` — หลักฐานตลาดจริงจากกลุ่ม Facebook รับจ้างถอดแบบ: ยืนยันราคา Concierge เดิม + เจอคู่แข่ง Phayoo Estimating (500 บาท/โครงการ/เดือน)

- เจ้าของโปรเจกต์ส่งภาพจริงจากกลุ่ม Facebook "รับจ้างถอดแบบ ประมาณราคา ทำ COST BOQ" วิเคราะห์สัดส่วนดีมานด์: ผู้รับเหมา ~60%, เจ้าของโครงการ ~30%, เจ้าของบ้าน ~10% — "เจ้าของโครงการ" เป็นกลุ่มรองที่มีนัยสำคัญ ยังไม่มี persona แยก (เพิ่มเป็น note ใน `01-market-competitors.md`)
- ราคาตลาดจริงของฟรีแลนซ์รับถอดแบบ: **10-15 บาท/ตร.ม.** (หรือ 5+5+5 บาท/ตร.ม. แยกหมวด) — **ตรงกับ illustrative price ของ Concierge MVP เดิม (1,500-3,000 บาท/หลัง) พอดี** สำหรับบ้าน < 200 ตร.ม. ยืนยันว่าตัวเลขเดิมสมเหตุสมผลจริง ไม่ใช่แค่เดา
- เจอคู่แข่งซอฟต์แวร์ไทยเพิ่ม: **Phayoo Estimating** (phayoo.com) — เครื่องมือวัด+ผูก formula คำนวณ BOQ อัตโนมัติ (คล้ายเป้าหมายระยะยาวของเราที่สุดเท่าที่เจอ) ราคา **500 บาท/โครงการ/เดือน** — เป็น comp ราคา subscription จริงชิ้นแรก ใช้เป็นจุดอ้างอิงตั้งราคา Founding Member (ควรต่ำกว่านี้อย่างมีนัยสำคัญ) — Phayoo ดูย้ายโฟกัสธุรกิจไปทาง AI Site Agent (LINE) แล้ว อาจเป็นโอกาสของเรา
- บันทึกรายละเอียดเต็มไว้ที่ [01-market-competitors.md §1.1](./01-market-competitors.md), [02-business-model-finance.md §1](./02-business-model-finance.md), [11-founding-member-pricing.md §0.1](./11-founding-member-pricing.md)

## 2569-08-26 (5)

`01-market-competitors.md`, `11-founding-member-pricing.md`, `BACKLOG.md` — ค้นราคาตลาดโลก+ไทยจริง เสนอปรับโมเดลราคา: เครื่องมือวัดฟรี ขายที่ฟีเจอร์เทียบ BOQ แทน

- ค้นราคาจริง: Bluebeam ($260-590/user/ปี), PlanSwift ($1,445-1,750 ครั้งเดียว หรือ $1,749/ปี), STACK (ฟรีจำกัด → $2,499/ปี), Togal.AI ($199-299/user/เดือน) — **โลกเลิกขาย one-time แล้วเกือบหมด หันไป subscription** เพราะมีต้นทุนดูแล/อัปเดตต่อเนื่องจริง
- ค้นราคา TumCivil เพิ่มเติม: ค่าสมาชิก VIP (ได้ TJ PDF ด้วย) = **500 บาทตลอดชีพ** — ราคานี้คือค่าสมัครเว็บทั้งหมด ไม่ใช่ราคาเฉพาะโปรแกรม โมเดลรายได้จริงมาจากชุมชน/คอนเทนต์ ไม่ใช่ขายเครื่องมือนี้ — เราสู้ราคาที่ชั้นนี้ไม่มีทางชนะ
- เสนอ (ยังไม่ตัดสินใจ): ทำเครื่องมือวัด v1 **ฟรี** ยอมรับว่าเป็น commodity แล้ว ย้ายจุดขายเงินไปที่ **subscription ฟีเจอร์เทียบปริมาณกับ BOQ อัตโนมัติ** (พิสูจน์คุณค่าแล้วกับเคส D5) ซึ่งไม่มีทั้ง TumCivil และคู่แข่งโลกทำ — ปรับ Founding Member deal จาก "ล็อกราคา one-time เครื่องมือวัด" เป็น "ล็อกราคา subscription ฟีเจอร์เทียบ BOQ ตลอดชีพ" แทน
- บันทึกรายละเอียดเต็มไว้ที่ [11-founding-member-pricing.md §0](./11-founding-member-pricing.md) และ [01-market-competitors.md §2.1](./01-market-competitors.md)

## 2569-08-26 (4)

`01-market-competitors.md`, `BACKLOG.md` — เจอคู่แข่งไทยตรงที่สุด: TumCivil.com "TJ PDF" แจกฟรีให้สมาชิก VIP

- เจ้าของโปรเจกต์ส่งโพสต์ Facebook จริงของ TumCivil.com (เว็บวิศวกรรมโยธาไทยที่มีฐานผู้ติดตามอยู่แล้ว) โฆษณาโปรแกรม "TJ PDF" — เปิด PDF/DXF/DWG, ตั้ง scale เอง, วัดยาว/พื้นที่/มุม/นับจำนวน, markup, ถอดปริมาณเข้า Excel, ทำงาน offline — **แจกฟรีให้สมาชิก VIP**
- ฟีเจอร์ตรงกับ MVP v1 ที่วางแผนไว้เกือบทุกจุด ต่างกันที่: (1) เป็น desktop app ไม่ใช่ web app (2) ไม่มีฟีเจอร์เทียบปริมาณกับ BOQ ที่ตีราคาไว้แล้ว (แค่ export ดิบ) (3) ไม่ผูกราคากลาง/มาตรฐานรหัสไทย
- ⚠️ กระทบโมเดล Founding Member โดยตรง — v1 ที่วางแผนขาย 990-1,490 บาท มีของฟรีทำสิ่งเดียวกันอยู่แล้ว เพิ่ม open item ใน `BACKLOG.md` ให้ทบทวนราคา/จุดขาย (อาจต้องย้ายไปขายที่ฟีเจอร์เทียบ BOQ ซึ่งยังไม่มีคู่แข่งทำ) — ยังไม่ตัดสินใจ รอเจ้าของโปรเจกต์
- บันทึกเป็นคู่แข่งใหม่ใน [01-market-competitors.md §1](./01-market-competitors.md)

## 2569-08-26 (3)

`10-pdf-mvp-spec.md` — ผู้เขียนนับ D4/D5 อิสระ ได้ตรงกับเจ้าของโปรเจกต์เป๊ะ, ไพน้อยแยก false positive ของ D4/D5 ไม่ได้เลย

- เจ้าของโปรเจกต์นับ D4/D5 จากแบบ A-04 ด้วยตา (วงกลม/สี่เหลี่ยม) ได้ D4=5, D5=4 — Claude นับอิสระแยกต่างหาก (ไล่ทีละ crop โดยไม่ดูผลเจ้าของโปรเจกต์ก่อน) ได้ผลตรงกันเป๊ะทั้งสองจำนวน — cross-validate ว่านับมือน่าเชื่อถือ
- รัน `pdf_door_label_matcher.py` ต่อกับ template D4/D5: ทั้งคู่ **แยก false positive ไม่ได้เลย** (คะแนนเกาะกลุ่มกันแน่นไม่มี threshold ตัดได้) แย่กว่า D1 (ซึ่งพอแยกได้บางส่วน 3/4) เพราะป้าย 2 ตัวอักษรแบบนี้คล้ายกันเกินไปข้ามรหัส (D2/D3/D4/D5/W3/W4)
- สรุป: ไม่มี threshold ตายตัวที่ใช้ได้ทั่วทั้งไฟล์ ต้องปรับ per-label ด้วยมือทุกครั้ง = ไม่มีประโยชน์ในทางปฏิบัติ ยืนยันหนักแน่นขึ้นว่า v1 ต้องเป็นคนวัดมือ 100% บันทึกไว้ที่ [10-pdf-mvp-spec.md §5.4](./10-pdf-mvp-spec.md)

## 2569-08-26 (2)

`08-ground-truth-boq-validation.md` — เจ้าของโปรเจกต์นับ D4/D5 จากแบบ A-04 ด้วยตา พบ BOQ นับ D5 ขาด 1 บาน

- เจ้าของโปรเจกต์วงกลม/สี่เหลี่ยมนับบานประตูจากแบบ A-04 เอง: D4 = 5 บาน, D5 = 4 บาน
- เทียบกับ BOQ (`Arch` 4.4/4.5): D4 ตรงเป๊ะ (5.0 ชุด) แต่ **D5 BOQ ตีราคาไว้แค่ 3.0 ชุด ทั้งที่แบบมี 4 บาน** — ขาด 1 ชุด (~12,000 บาท)
- บันทึกไว้เป็นหลักฐานที่ 2 ของความคลาดเคลื่อนใน BOQ จริง (ต่อจาก code ซ้ำใน §7) — คนละประเภท คือ**นับปริมาณขาดจริง** ไม่ใช่แค่พิมพ์ผิด ยังไม่ฟันธงสาเหตุ (นับพลาด/อ่านป้ายผิด/ตั้งใจ) ต้องตรวจสอบเพิ่มกับไฟล์ต้นฉบับหรือหน้างานจริง
- เป็นตัวอย่างรูปธรรมว่า MVP v1 (วัดมือ+เทียบ BOQ) มีคุณค่าจับความคลาดเคลื่อนแบบนี้ได้เลย ไม่ต้องรอ AI Phase 2 — บันทึกละเอียดที่ [08-ground-truth-boq-validation.md §7.1](./08-ground-truth-boq-validation.md)

## 2569-08-26

`project/code/python/pdf_door_label_matcher.py` (ใหม่), `10-pdf-mvp-spec.md`, `BACKLOG.md` — ทดสอบนับรายการจริง 1 ตัวอย่างจาก PDF สแกน: วัดมือ 4/4 ตรง BOQ, อัตโนมัติได้ 3/4

- ตั้งใจทดสอบหมวด 1 รายการ 1 ใน BOQ ("รางวัดขอบเขตที่ดิน") แต่พบว่าเป็นงานเหมา ไม่มีปริมาณเชิงเรขาคณิต (ทั้งหมวด 1 เป็นแบบนี้) — ถามเจ้าของโปรเจกต์แล้วเปลี่ยนไปทดสอบ Arch sheet 4.1 "D1 ประตูบานเลื่อนอลูมิเนียม -- 4.0 ชุด" แทน (นับง่าย ตรวจสอบง่ายที่สุด)
- วัดมือ (จำลอง MVP v1): เปิดหน้า A-04 (แปลนพื้น) เทียบหน้า A-10 (แบบขยายประตู) นับป้าย "D1" ได้ **4 จุด ตรงกับ BOQ เป๊ะ**
- เขียน `pdf_door_label_matcher.py` ให้ "ไพน้อย" ลองนับอัตโนมัติจากภาพสแกน (ไม่มี OCR/OpenCV/scipy ในเครื่อง) — ใช้ normalized cross-correlation ผ่าน FFT ล้วนๆ จับคู่ template ป้าย D1
- ผลลัพธ์: เจอ 3/4 จุดด้วยความมั่นใจสูงมาก (score 0.97–1.00) แต่จุดที่ 4 (ป้ายติดสัญลักษณ์ลูกศร+เส้นผนัง) แยกจาก false positive อื่นๆ ไม่ได้เลยไม่ว่าจะปรับ threshold/ครอป template แน่นแค่ไหน — ลองใช้จุดที่ 4 เองเป็น template ก็พบว่า template นั้น "เสีย" จากกราฟิกที่ทับซ้อนตั้งแต่ต้น (443 matches มั่ว)
- สรุป: นี่คือหลักฐานเชิงประจักษ์ชิ้นแรก (ไม่ใช่แค่คาดเดา) ที่ยืนยันว่า template matching ธรรมดาไม่พอสำหรับ auto-recognize จาก PDF สแกน ต้องใช้ OCR/ML จริงถ้าจะทำ Phase 2 — ยืนยันการตัดสินใจเดิมว่า v1 ต้องเป็นวัดมือ บันทึกละเอียดไว้ที่ [10-pdf-mvp-spec.md §5.3](./10-pdf-mvp-spec.md)

## 2569-08-25 (3)

`10-pdf-mvp-spec.md` — ตัดสินใจทิศทาง: สแกนกระดาษมีตราประทับเทศบาล = เคสหลักของ MVP ไม่ใช่ vector PDF

- เจ้าของโปรเจกต์ชี้ทิศทางใหม่: ยึด**แบบก่อสร้างที่ได้รับใบอนุญาตจริงจากเทศบาล มีตราประทับ+ลายเซ็นรับรอง**เป็นต้นแบบหลักของ MVP แม้จะเป็น PDF ภาพสแกน (ไม่ใช่ vector) เพราะนี่คือสิ่งที่ผู้รับเหมา/เจ้าของบ้านมีอยู่จริงในสัดส่วนส่วนใหญ่ (~90% ตามประสบการณ์จริง) — ขอไม่ยุ่งกับ Kadfarang อีก (ปิดประเด็นนั้นไว้เฉยๆ)
- render หน้า 1 ของ `แบบก่อสร้างอาคารบ้านพักอาศัยชั้นเดียว...pdf` ออกมาดูด้วยตา ยืนยันภาพจริง: ตราประทับ "เทศบาลตำบลสันปูเลย", กล่องใบอนุญาตเลขที่ 503-2568, ลายเซ็นจริงครบ 5 ตำแหน่ง, ที่อยู่ตรงกับโปรเจกต์ newhouse (ต.สันปูเลย อ.ดอยสะเก็ด จ.เชียงใหม่) — ยืนยันไฟล์นี้คือแบบที่ต้องการพอดี
- ตั้งไฟล์นี้เป็น**ต้นแบบ (prototype reference) อย่างเป็นทางการ**สำหรับพัฒนา/ทดสอบ MVP v1 ต่อจากนี้ — มี ground truth คู่กันพร้อมใช้ทันที (`BOQ - new house.xls`, โปรเจกต์เดียวกัน)
- กลับลำดับความสำคัญ item ใน [10-pdf-mvp-spec.md §7](./10-pdf-mvp-spec.md) (เดิมเขียนผิดทางไว้ว่า "เริ่มจาก vector PDF ก่อนเพื่อความง่าย") — ปิด item นั้นแล้ว

## 2569-08-25 (2)

`project/code/python/pdf_inspector.py` (ใหม่), `10-pdf-mvp-spec.md`, `BACKLOG.md` — ไฟล์ที่ plot ออกมากลับเป็นภาพสแกน ไม่ใช่ vector แต่ไม่กระทบ MVP v1

- เจ้าของโปรเจกต์ plot `newhouse 2569.cleaned.v4.dxf` ตามที่แนะนำไว้ ได้ไฟล์ `project/new house/PDF/แบบก่อสร้างอาคารบ้านพักชั้นเดี่ยว.pdf` (29 หน้า) — ตรวจแล้วพบว่าเป็น **ภาพสแกนกระดาษล้วน** (metadata ระบุ `Scanner System Image Conversion`) ไม่ใช่ vector PDF ที่ตั้งใจทดสอบ (0 vector, 0 ข้อความ, 0 OCG ทุกหน้า, ภาพ ~212dpi/หน้า)
- เขียน [`pdf_inspector.py`](./project/code/python/pdf_inspector.py) เป็นเครื่องมือตรวจสอบทั่วไป จัดประเภทหน้า PDF เป็น `vector_with_text`/`vector_no_text`/`raster_scan`/`empty` อัตโนมัติ — รันเทียบกับผลตรวจ manual (PyMuPDF ตรงๆ) ได้ผลตรงกันทุกตัวเลขตั้งแต่รันแรก ไม่ต้องแก้อะไร
- ทำตารางเปรียบเทียบ 3 ตัวอย่างจริงที่เจอมา (สแกนกระดาษ / vector ระเบิด Kadfarang / DXF ต้นฉบับ) ไว้ใน [10-pdf-mvp-spec.md §5.1](./10-pdf-mvp-spec.md) — **ข้อสรุปสำคัญ:** แม้ไฟล์นี้จะแย่ที่สุดในแง่ข้อมูลดิบ (แย่กว่า Kadfarang ด้วยซ้ำ — ไม่มี vector เลย) แต่**ไม่กระทบ MVP v1 เลย** เพราะ v1 วัดจากภาพที่ render บนจอ ไม่ได้อ่านโครงสร้างข้อมูลเบื้องหลัง — ปิด open item ใน `BACKLOG.md` เรื่อง "รับ PDF ภาพสแกนไหม" ได้บางส่วน (ยังเหลือคำถาม DPI ขั้นต่ำที่ยอมรับได้)
- ยังไม่ได้ตัวอย่าง PDF vector+ข้อความจริงจาก CAD (ต้องลองใหม่ ตรวจให้แน่ใจว่า driver ที่ใช้ plot ไม่ได้ไปออกที่เครื่องสแกน)

## 2569-08-25

`10-pdf-mvp-spec.md` — ตรวจไฟล์ PDF ลูกค้าจริงเพื่อตอบคำถาม "เอาหลักการ DXF มาใช้กับ PDF อัตโนมัติได้ไหม" — สรุป: ยังไม่ได้ ยืนยัน v1 manual-only

- เจ้าของโปรเจกต์ถามว่า 4 ขั้นตอนของ PDF MVP (Upload → Calibrate Scale → Measure → Export) ให้ "ไพน้อย" ทำอัตโนมัติได้ไหม โดยพอร์ตหลักการ signal-vs-noise จากงาน DXF มาใช้กับ PDF แทน
- ตรวจไฟล์จริง `project/welcome maerem/cad/แบบกาดเวลค่ำกาดฝรั่งแม่ริม.pdf` (ไฟล์ต้นทางของ Kadfarang.dxf ที่ยืนยันแล้วว่าใช้ไม่ได้) ด้วย PyMuPDF: 6 หน้าแรกมี vector object 7,792–35,355 ชิ้น/หน้า แต่ **extract ข้อความได้ 0 ตัวอักษรทุกหน้า**, **OCG (layer) = 0**, path เป็นเส้นตรงล้วน (**ไม่มี bezier curve เลยแม้แต่ชิ้นเดียว**) — สรุปว่าไฟล์นี้ถูก tessellate ทุกอย่างรวมถึงตัวอักษรให้เป็นเส้นเปล่าไม่มีป้ายกำกับ อธิบายได้ว่าทำไมแปลงเป็น DXF แล้วถึงกลายเป็นเส้นล้วนไร้โครงสร้าง
- สรุปคำตอบ: เทคนิค DXF ทั้งหมด (layer exclusion, keyword matching, evidentiary threshold) ใช้กับไฟล์ประเภทนี้ไม่ได้เลยเพราะไม่มี layer name และไม่มีข้อความให้อ่าน จะ auto-recognize ผนัง/ประตูจาก PDF แบบนี้ต้องใช้ computer-vision/ML รู้จำผังพื้น ซึ่งเป็นโปรเจกต์วิจัยที่ใหญ่กว่างาน DXF ทั้งหมดรวมกัน — ไม่ควรผูกกับ MVP
- ⚠️ จำกัดเฉพาะไฟล์นี้ (n=1, จาก pipeline export ที่รู้อยู่แล้วว่าแย่เป็นพิเศษ) — PDF ที่ export ด้วย driver ดีกว่าอาจมีข้อความ+layer จริง ยังไม่เคยตรวจเทียบ (เพิ่มเป็น open item ใน `10-pdf-mvp-spec.md` §7)
- บันทึกหลักฐาน+ข้อสรุปทั้งหมดไว้ใน [10-pdf-mvp-spec.md §5](./10-pdf-mvp-spec.md) เป็นส่วนใหม่ ยืนยันการตัดสินใจเดิม (v1 manual-only) ด้วยหลักฐานจริงแทนที่จะเป็นแค่ทางเลือกที่ปลอดภัยกว่า

## 2569-08-24

`10-pdf-mvp-spec.md` (ใหม่), `11-founding-member-pricing.md` (ใหม่), `06-roadmap.md`, `README.md`, `BACKLOG.md` — **ปรับทิศทางใหญ่: PDF MVP มาก่อน DWG**

- งานวิจัยตลาด ([01-market-competitors.md](./01-market-competitors.md), ทำเพิ่มเติมวันนี้) ยืนยันว่า **PDF คือรูปแบบไฟล์ที่ผู้รับเหมาได้รับจริงมากที่สุด ไม่ใช่ DWG** — CAD ใช้เยอะแค่ระหว่างออฟฟิศออกแบบด้วยกันเอง พอถึงมือผู้รับเหมามักถูกแปลงเป็น PDF แล้ว ส่วน BIM/Revit แทบไม่มีใช้เลยในกลุ่มเป้าหมาย (งานวิจัยไทยยืนยัน BIM ใช้จริงแค่โครงการรัฐ/ใหญ่)
- เจ้าของโปรเจกต์ยืนยันจากประสบการณ์จริง: ได้ PDF มาต้อง **print ลงกระดาษก่อนถึงจะได้สเกลที่เชื่อถือได้** แล้วค่อยนั่งถอดแบบ ใช้เวลา 2-3 สัปดาห์ต่อโปรเจกต์ — pain point จริงคือ "หาสเกลไม่ได้" ไม่ใช่ "นับช้า"
- เสนอกลยุทธ์ไปตลาดแบบ Minecraft alpha: ขายเวอร์ชันยังไม่สมบูรณ์ตอนนี้เลย ล็อกราคาถูกให้ 100 คนแรก จ่ายครั้งเดียว ได้อัปเกรด AI ในอนาคตฟรีตลอดไป
- เขียน [10-pdf-mvp-spec.md](./10-pdf-mvp-spec.md): MVP = calibrate scale ด้วยมือ (คลิก 2 จุดบนเส้นบอกขนาดที่รู้ค่า) + วัดเส้น/พื้นที่/นับจำนวนด้วยมือ — **ไม่มี AI เลยใน v1** ใช้หมวดหมู่จาก `04-category-schema.md` ได้ทันที (format-agnostic) ระบุจุดเสี่ยงสำคัญ: ต้องรองรับหลายสเกลในหน้าเดียว (แบบขยายมักคนละสเกลกับแปลนหลัก)
- เขียน [11-founding-member-pricing.md](./11-founding-member-pricing.md): กลไกราคา 100 คนแรก จ่ายครั้งเดียว (illustrative ~990-1,490 บาท ยังไม่ยืนยัน) — ⚠️ เตือนไว้ชัดเจนว่าคำมั่น "อัปเกรดฟรีตลอดชีพ" ต้องระบุขอบเขต (ฟีเจอร์ซอฟต์แวร์ฟรีได้ แต่ฟีเจอร์ที่มีต้นทุนแปรผันจริงในอนาคตควรมี fair-use limit) และห้ามตั้งกำหนดเวลาชัดเจนให้ AI feature เพราะประสบการณ์จริงจากงาน DWG (Duplicate Purge ไม่แก้ WALL, evidence-scoring แยกกรณีก้ำกึ่งไม่ได้) แสดงให้เห็นว่าใช้เวลานานกว่าคาดเสมอ
- อัปเดต `06-roadmap.md`: PDF MVP เป็น Phase 1 ใหม่ (เส้นทางเร็วที่สุด) DWG กลายเป็น Phase 1b ที่ทำขนานกันไปแบบไม่เร่งรีบ ไม่ใช่ทางลัดแรกอีกต่อไป

## 2569-08-23 (20)

`project/code/python/clean_dxf.py`, `07-drawing-signal-vs-noise.md`, `BACKLOG.md` — แก้บั๊กสำคัญ: style table เปลี่ยนแล้วแต่ฟอนต์บางส่วนยังไม่เปลี่ยนจริง

- เจ้าของโปรเจกต์ส่งภาพหน้าจอ GstarCAD เปิดไฟล์ v3 มาให้ดู — พบข้อความไทยบางส่วนแตกเป็นชิ้นๆ อ่านไม่ออก ทั้งที่ตรวจ style table แล้วยืนยันว่าเป็น CordiaUPC.ttf ครบทุก style
- ตรวจสอบพบสาเหตุจริง: **MTEXT ฝังคำสั่งเปลี่ยนฟอนต์ไว้ในเนื้อความของตัวเองได้** (`{\fTH SarabunPSK|b0|i0|c222|p34;...}`) ซึ่งมีผลเหนือกว่า font ที่ style table กำหนด — `standardize_fonts()` แก้แค่ style table เลยไม่มีผลกับข้อความกลุ่มนี้เลย
- ตรวจทั้งไฟล์พบ **179 MTEXT ที่ฝัง font override ไว้ อ้างอิง 7 ฟอนต์ต่างกัน** (`TH SarabunPSK`, `LilyUPC`, `AngsanaUPC`, `Vrinda`, `Cordia New`, `Angsana New`, `FreesiaUPC`) — ตัวที่แตกในภาพคือกลุ่มที่ฝัง `TH SarabunPSK` พอดี (ฟอนต์ที่เครื่องเจ้าของโปรเจกต์ไม่มี)
- เพิ่ม `strip_inline_font_overrides()` ลบคำสั่ง `\f...;` ที่ฝังอยู่ทั้งหมดออก (ไม่ใช่เปลี่ยนชื่อฟอนต์ในคำสั่ง — ลบทิ้งให้กลับไปใช้ font ของ style ตัวเอง) รันคู่กับ `--standardize-font` เสมอ ไม่ใช่ pass แยก
- รันจริง: ลบได้ครบ 179/179 จุด ตรวจซ้ำแล้วว่าไม่เหลือเลย ได้ไฟล์ `newhouse 2569.cleaned.v4.dxf`
- บทเรียนสำคัญ: ตรวจแค่ `doc.styles` ด้วยโค้ดไม่พอจะสรุปว่าฟอนต์ถูกหมดแล้ว ต้องเปิดดูด้วยตาจริงเสมอ

## 2569-08-23 (19)

`project/code/python/clean_dxf.py`, `07-drawing-signal-vs-noise.md`, `BACKLOG.md` — เพิ่ม `--purge-styles` เข้า clean_dxf.py

- เจ้าของโปรเจกต์ถามว่ามี purge style ที่ไม่ได้ใช้ไหม — เพิ่ม `purge_unused_styles()` ใช้ตรรกะเดียวกับ purge เลเยอร์: เช็ค TEXT/MTEXT/ATTDEF/ATTRIB ทุกตัวในทุก layout/block **บวกกับ `dimtxsty` ของทุก DIMSTYLE** (เพราะ style อาจถูกใช้แค่ในระบบ dimension โดยไม่มี TEXT ตรงอ้างอิงเลย) ป้องกัน `STANDARD` ไม่ให้ถูกลบเหมือนที่ป้องกันเลเยอร์ `0`
- รันจริงกับ `newhouse 2569.dxf`: ลบได้ 11 style ที่ไม่ใช้จริง (เช่น `GOST-2.304_Type-B` 3 ตัวที่ไม่มี font เลยด้วยซ้ำ) เหลือ 55/66 style ให้ `standardize_fonts()` ทำงานต่อ — ได้ไฟล์ `newhouse 2569.cleaned.v3.dxf`

## 2569-08-23 (18)

`07-drawing-signal-vs-noise.md` — เปลี่ยนฟอนต์มาตรฐานจาก THSarabunPSK.ttf เป็น CordiaUPC.ttf ตามคำขอ

- เจ้าของโปรเจกต์แจ้งว่าไม่มีฟอนต์ THSarabunPSK.ttf ในเครื่องจริง (ต่างจากที่ตอบไว้ก่อนหน้า) — รันใหม่ด้วย `--standardize-font "CordiaUPC.ttf"` แทน ยืนยันแล้วว่า 66/66 style เป็น CordiaUPC.ttf หมด (47 เปลี่ยนจริง, 19 ใช้ฟอนต์นี้อยู่แล้ว) — auto-versioning ทำงานถูกต้อง ได้ไฟล์ `newhouse 2569.cleaned.v2.dxf` ไม่ทับ v1

## 2569-08-23 (17)

`project/code/python/clean_dxf.py`, `.gitignore`, `07-drawing-signal-vs-noise.md` — เพิ่ม auto-versioning ให้ output ของ clean_dxf.py ไม่ทับไฟล์เดิมอีกต่อไป

- ปัญหาที่เจอ: ส่งไฟล์ cleaned ชื่อเดิม (`*.cleaned.dxf`) ให้ตรวจ 2 รอบ เพราะสคริปต์เขียนทับไฟล์เดิมทุกครั้งที่รัน เจ้าของโปรเจกต์เลยแยกไม่ออกว่าไฟล์ที่เปิดมีฟอนต์แก้แล้วหรือยัง (ยืนยันแล้วว่าฟอนต์ถูกแก้จริงในไฟล์บนดิสก์ — น่าจะเป็นปัญหาการแยกเวอร์ชันไฟล์ ไม่ใช่โค้ดพัง)
- เพิ่ม `next_versioned_output_path()`: default output เปลี่ยนจาก `<name>.cleaned.dxf` (ทับทุกครั้ง) เป็น `<name>.cleaned.v1.dxf`, `.v2.dxf`, ... หาเลขเวอร์ชันสูงสุดที่มีอยู่แล้ว +1 อัตโนมัติ — ไม่ทับของเก่าอีกต่อไป เทียบก่อน-หลังได้ตลอด
- เพิ่ม pattern `*.cleaned.v*.dxf` เข้า `.gitignore` (ยังเป็น derived artifact เหมือนเดิม ไม่ commit)
- รันใหม่ยืนยัน: `newhouse 2569.cleaned.v1.dxf` มี 66/66 text style เป็น THSarabunPSK.ttf ถูกต้อง

## 2569-08-23 (16)

`project/code/python/clean_dxf.py`, `07-drawing-signal-vs-noise.md`, `BACKLOG.md` — เพิ่ม 2 pass ใหม่ (opt-in): มาตรฐานฟอนต์ + purge เลเยอร์ไม่ใช้ ตามที่เจ้าของโปรเจกต์ขอหลังตรวจไฟล์ cleaned รอบแรก

- `--standardize-font` (default `THSarabunPSK.ttf`): เซ็ตทุก text style ให้ใช้ฟอนต์เดียวกัน — **ยืนยันปัญหาจริงตามที่สงสัย**: ไฟล์มี 66 text style ใช้ฟอนต์ต่างกันเป็นสิบแบบ (`CordiaUPC.ttf` ผูกกับชื่อ style เกือบสิบชื่อ, `.shx` หลายแบบ, แม้แต่ path เพี้ยนๆ `C:\AECPLUS/fonts/SIMPLEX` และ `-DB 75 Narai`) — รันจริงเปลี่ยนครบ 66/66 style
- `--purge-layers`: ลบเลเยอร์ที่ไม่มี entity ไหนใช้เลย (เช็ค modelspace + ทุก layout + ทุก block definition) — รันจริงลบได้ 28 เลเยอร์ (เช่น ASHADE, SPACE, STAIR, HACTH, บ้าน, ลำน้ำ) ตรงกับที่สงสัยว่าติดมาจากไฟล์โครงการอื่น
- ทั้งสอง pass เป็น opt-in (ปิดโดย default) เพราะเป็นการแก้ไขเนื้อหาแบบจริง ไม่ใช่แค่คัดกรองขยะ
- **เรื่อง plot style (`.clb`):** ตรวจไฟล์แล้วพบว่า `current_style_sheet` ของทั้ง Model และ Layout1 ตั้งเป็น `monochrome.ctb` อยู่แล้ว ตรงกับที่ขอพอดี — `.clb` ไม่ใช่นามสกุลมาตรฐานของ AutoCAD (ปกติ `.ctb`/`.stb`) ยังไม่แก้อะไร รอถามเจ้าของโปรเจกต์ให้ชัดก่อน

## 2569-08-23 (15)

`project/code/python/clean_dxf.py`, `07-drawing-signal-vs-noise.md` — เติม Duplicate Purge ให้ครอบคลุม TEXT/MTEXT

- เจ้าของโปรเจกต์ถามว่า "คำซ้ำๆ ในตำแหน่งเดิม (เช่น ป้ายบอกขนาดท่อน้ำ)" อยู่ขั้นตอนไหน — ตรวจพบว่า Duplicate Purge เดิมเช็คแค่ LINE/LWPOLYLINE ไม่ได้แตะ TEXT/MTEXT เลย เป็นช่องโหว่จริง เพิ่มให้ครอบคลุมแล้ว (ซ้ำก็ต่อเมื่อทั้งคำและตำแหน่งตรงกันเป๊ะ)
- ย้ำข้อควรระวัง: ป้ายขนาดท่อ (เช่น "CW Ø3/4"") ที่ซ้ำคำแต่คนละตำแหน่ง **ไม่ใช่ของซ้ำ** เพราะเป็นท่อคนละเส้นจริง — ต้องต่างตำแหน่งด้วยถึงจะไม่ลบ
- ทดสอบกับ `newhouse 2569.dxf`: ไม่มี TEXT/MTEXT ซ้ำจริงในไฟล์นี้เลย (0 รายการ) — โค้ดพร้อมใช้แล้ว รอไฟล์อื่นที่มีเคสจริงมาทดสอบ

## 2569-08-23 (14)

`04-category-schema.md`, `project/code/python/pynoi_parser.py`, `project/code/python/clean_dxf.py`, `07-drawing-signal-vs-noise.md`, `BACKLOG.md` — แก้ไขสำคัญ: "ไม่เกี่ยวกับ BOQ" ≠ "ไม่เกี่ยวกับแบบ"

- เจ้าของโปรเจกต์ทักท้วงหลังทำ step-by-step demo (Step 1-3): `clean_dxf.py` ลบเลเยอร์อย่าง DIM/GRID/TITLE/SPEC/HATCH/ของตกแต่งออกจากไฟล์จริง โดยคิดว่าไม่เกี่ยวข้อง — **แต่สิ่งเหล่านี้เกี่ยวข้องกับตัวแบบจริงๆ แค่ไม่ใช่ปริมาณที่ต้องนับใน BOQ** การลบออกจากไฟล์จริงคือทำลายแบบ ไม่ใช่แค่ทำความสะอาด
- แยก `exclusion_rules` ใน `04-category-schema.md` เป็น 2 ระดับ: `quantity_exclusion` (ข้ามตอนนับปริมาณ แต่ **ห้ามลบ** — DIM/GRID/TITLE/SPEC/HATCH/ของตกแต่ง/เลเยอร์ `0`/เลเยอร์ตั้งชื่อตามน้ำหนักเส้น) และ `safe_to_delete` (ลบได้จริง เพราะเป็น software-internal ล้วนๆ ไม่มีข้อมูลมนุษย์สร้างเลย — `DEFPOINTS` ที่ไม่เคยพิมพ์ออกมาแม้แต่ครั้งเดียว, block/เลเยอร์ที่ AutoCAD/ปลั๊กอินสร้างเองอัตโนมัติ)
- เพิ่ม `is_safe_to_delete()` ใน `pynoi_parser.py` (แคบกว่า `is_excluded_layer()` เดิมมาก) — `clean_dxf.py` เปลี่ยนไปใช้ฟังก์ชันนี้แทน
- **ผลลัพธ์:** รันซ้ำกับ `newhouse 2569.dxf` ลบจาก 6,600 เหลือ **787 entity** เท่านั้น (48 เลเยอร์ software-internal, 130 block software-internal, 609 ของซ้ำจริง) ตรวจยืนยันแล้วว่า DIM/GRID/SPEC ยังอยู่ในไฟล์ที่ล้างแล้วครบถ้วน — เขียนสรุปเต็มไว้ใน [07-drawing-signal-vs-noise.md §15](./07-drawing-signal-vs-noise.md#15-️-แก้ไขสำคัญ-ไม่เกี่ยวกับ-boq--ไม่เกี่ยวกับแบบ--สรุปเรื่องการกรองใหม่ทั้งหมด) ตามที่เจ้าของโปรเจกต์ขอให้สรุปการกรองใหม่

## 2569-08-23 (13)

`project/code/python/clean_dxf.py`, `07-drawing-signal-vs-noise.md`, `BACKLOG.md` — เพิ่ม Duplicate Purge เข้า clean_dxf.py ตามคำขอ "ลบคำซ้ำๆ"

- เพิ่ม pass ที่ 2 ใน `clean_dxf.py`: เทียบ LINE (จุดเริ่ม/จบ ไม่สนทิศทาง) และ LWPOLYLINE (vertex list ไม่สนทิศทาง) ที่ปัดเศษแล้วตรงกันบนเลเยอร์เดียวกัน เก็บตัวแรกไว้ ลบตัวที่เหลือ — ปรับ tolerance ได้ผ่าน `--dup-tolerance`
- รันจริงกับ `newhouse 2569.dxf`: ลบซ้ำได้ 300 entity แต่ **พื้นที่เลเยอร์ WALL แทบไม่เปลี่ยน (289,034.51 → 289,033.50 ตร.ม.)** สรุปตรงๆ ว่า Duplicate Purge ไม่ใช่คำตอบของปัญหานี้ — ของที่ปนกันอยู่ไม่ใช่เส้นซ้ำ แต่เป็นรูปปิดคนละรูปทรงจริงหลายรูป (hatch/detail loop) ปนกันในเลเยอร์เดียว ต้องใช้ Bounding Box Filter ต่อ (ยังไม่เริ่มทำ)

## 2569-08-23 (12)

`project/code/python/evidence_score.py` (ใหม่), `07-drawing-signal-vs-noise.md`, `BACKLOG.md` — implement เกณฑ์ "หลักฐานสนับสนุน" เป็นโค้ด พบข้อจำกัดจริงระหว่างทดสอบ

- เขียน `evidence_score.py` ทำเกณฑ์ข้อ 2 (ข้อความบรรยายใกล้ๆ) + ข้อ 3 (วางซ้ำสม่ำเสมอ) จาก §14 — ยังไม่ทำข้อ 1 (จับคู่ Legend/Schedule) เพราะต้องรอ legend parsing ก่อน
- รอบแรก filter ข้อความ "descriptive" หลวมเกินไป — `ct0150p` (ที่ยืนยันว่าเป็นความผิดพลาด) ติด false positive จาก "MH." (ชื่อโครงการย่อ) และ "CW Ø3/4"" (ป้ายบอกขนาดท่อ) แก้ filter ให้ตัดรูปแบบตัวย่อสั้นๆ และป้ายบอกขนาด/สเปกออกแล้ว
- **แก้แล้วยังพบข้อจำกัดจริงอีกชั้น:** `ct0150p` ยังคงติด "likely_signal" ผ่านเกณฑ์ข้อ 3 อยู่ดี เพราะถูกวางซ้ำ 12 ครั้งบนเลเยอร์เดียวกันสม่ำเสมอ — เหมือนของจริง (`C805P`/`CT002P`/`DR090SL1`) ทุกประการ เพราะดราฟต์แมนทำผิดแบบเดิมซ้ำๆ ไม่ใช่ผิดครั้งเดียว **สรุปตรงๆ ว่าเกณฑ์ 2-3 แยกเคสนี้ไม่ออกจริง** ใช้กรองขยะชัดเจนได้ (เช่น `B-3` ที่ไม่มีหลักฐานอะไรเลย ให้ผล insufficient_evidence ถูกต้อง) แต่กรณีก้ำกึ่งยังต้องพึ่งมนุษย์ยืนยันเหมือนเดิม ไม่ใช่ auto-apply — บันทึกไว้ตรงๆ ไม่ปิดบังข้อจำกัด

## 2569-08-23 (11)

`04-category-schema.md`, `07-drawing-signal-vs-noise.md`, `BACKLOG.md`, `vocabulary_review.csv` — เจ้าของโปรเจกต์แก้ทิศทางสำคัญ: เลิกไล่ยืนยันรหัสสินค้าทีละตัว เปลี่ยนเป็นเกณฑ์ "หลักฐานสนับสนุน"

- เจ้าของโปรเจกต์ชี้ว่าการไล่ยืนยัน COTTO code ทีละตัว (round ก่อนหน้า) **ไม่ scale ข้ามโครงการ/บริษัท** — ยกตัวอย่างจริง: `ct0150p` อยู่บนเลเยอร์ `DIM` เหมือน `C805P` ที่ยืนยันแล้วว่าเป็นสัญญาณจริง แต่ `ct0150p` กลับเป็น**ความผิดพลาดของดราฟต์แมนคนหนึ่งที่วางผิดเลเยอร์เฉยๆ** (ให้พนักงานคนนั้นออกไปแล้ว 2 เดือนก่อน) — พิสูจน์ว่า "อยู่เลเยอร์เดียวกัน" ไม่ใช่หลักฐานที่เพียงพอเสมอไป
- **หลักการใหม่ที่เจ้าของโปรเจกต์วางไว้:** "เห็นลึกกว่ามนุษย์ได้ แต่ถ้ามนุษย์มองแล้วไม่เห็นว่าเกี่ยวข้อง ก็ไม่ต้องนับเข้า BOQ" — กลับภาระการพิสูจน์: **default เป็น "ไม่นับ" จนกว่าจะมีหลักฐานสนับสนุนอย่างน้อย 1 ข้อ** (ตรงกับ Legend/Schedule, มีข้อความบรรยายชัดเจนใกล้ๆ, หรือรูปแบบวางซ้ำสม่ำเสมอ) แทนที่จะพยายามสืบจนรู้ว่ามันคืออะไรเป๊ะแบบเดิม — เขียนเป็นหลักการเต็มใน [07-drawing-signal-vs-noise.md §14](./07-drawing-signal-vs-noise.md#14-️-กลับหลักการ-ต้องมีหลักฐานสนับสนุนก่อนถึงจะนับเป็นสัญญาณ-ไม่ใช่นับไว้ก่อนแล้วค่อยพิสูจน์ว่าเป็นขยะ)
- ปิดกลุ่ม `confirmed_brand_product_codes` ใน `04-category-schema.md` ด้วยเกณฑ์นี้ทีเดียว (ไม่ไล่ทีละตัวอีก): `C812F` (หลักฐานสูง มีข้อความ "TOWEL BAR" ชัดเจน), `C106P`/`C112S`/`C814F`/`C814S` (พอเชื่อได้จากชื่อเลเยอร์), `C053S`/`CT158AP`/`CT767S`/`ct1113ap` (ไม่ได้ใช้จริงในแบบ ไม่เกี่ยว), `c0156f`/`c805f`/`ct268f` (หลักฐานขัดกับสมมติฐานสุขภัณฑ์ ไม่ควรเดา), `ct0150p` (ยืนยันแล้วว่าเป็นความผิดพลาดเฉพาะไฟล์นี้ ใช้ `action=ignore` ไม่ใช่ `exclude` เพื่อไม่ให้รหัสเฉพาะไฟล์นี้หลุดเข้า global denylist ถาวร)
- backlog: implement เกณฑ์นี้เป็นโค้ดจริงยังไม่ได้ทำ (ยังเป็นแค่หลักการ)

## 2569-08-23 (10)

`project/code/python/clean_dxf.py` (ใหม่), `07-drawing-signal-vs-noise.md`, `.gitignore`, `BACKLOG.md` — ตอบคำถาม "ล้างไฟล์ก่อนค่อยถอดแบบดีไหม" ด้วยการ implement จริง + ปิดประเด็นไฟล์ที่สอง

- เจ้าของโปรเจกต์ยืนยันว่า `Kadfarang Drawing.dxf` เป็นไฟล์ PDF ระเบิดเส้น ไม่มีโครงสร้างมีความหมาย ไม่ใช้เป็นไฟล์ทดสอบตัวที่สอง — ตรงกับกฎที่มีอยู่แล้วใน SOP Step 1 พอดี (backlog ปิดประเด็นนี้แล้ว, ยังต้องหาไฟล์ CAD ดิจิทัลแท้ตัวใหม่มาแทน)
- ตอบคำถาม "ล้างไฟล์ก่อนถอดแบบดีไหม": **ทำทั้งคู่ ไม่ใช่เลือกอย่างเดียว** — เขียน `clean_dxf.py` ใช้ logic เดียวกับ Layer Exclusion materialize เป็นไฟล์ `*.cleaned.dxf` จริง (ไม่แตะไฟล์ต้นฉบับ, entity-type-aware ตาม entity_type_warning — INSERT ตัดสินจากชื่อ block ไม่ใช่เลเยอร์) เหตุผล 3 ข้อ: เปิดดูด้วยตาได้จริง, เป็น deliverable ของ CAD Cleanup Fee ได้ตรงๆ, เป็นจุด checkpoint ก่อนคำนวณที่ซับซ้อนกว่า
- **รันจริงกับ `newhouse 2569.dxf`:** ลบ 6,300/18,043 entity (~35%), ไฟล์เล็กลง 21.6MB→18.6MB — แต่ตรวจซ้ำแล้วพบว่า **เลเยอร์ `WALL` ยัง sum พื้นที่ได้ 289,034.51 ตร.ม. เท่าเดิมทุกประการ** เพราะปัญหานั้นเป็นปัญหาระดับเรขาคณิต (hatch ปนอยู่ในเลเยอร์เดียวกับผนังจริง) ไม่ใช่ปัญหาระดับชื่อเลเยอร์ — สรุปตรงๆ ว่าการล้างไฟล์รอบนี้เป็นก้าวที่ดีแต่ยังไม่พอ ต้องทำ Duplicate Purge + Bounding Box Filter ต่อ (backlog เลื่อนความสำคัญขึ้นเป็นอันดับแรก)
- ไฟล์ `*.cleaned.dxf` ที่สร้างจากสคริปต์นี้ ไม่ commit เข้า git (เป็น derived artifact สร้างซ้ำได้เสมอจากไฟล์ต้นฉบับ + schema ปัจจุบัน) — เพิ่ม pattern เข้า `.gitignore`

## 2569-08-23 (9)

`04-category-schema.md`, `07-drawing-signal-vs-noise.md`, `09-vocabulary-review-workflow.md` (ใหม่), `project/code/python/schema_loader.py` (ใหม่), `vocabulary_review.py` (ใหม่), `apply_vocabulary_review.py` (ใหม่), `pynoi_parser.py`, `classify_layers.py`, `README.md`, `BACKLOG.md` — เจ้าของโปรเจกต์ยืนยันคำศัพท์ + ถามเรื่องระบบลดภาระการสอนคำศัพท์

- ยืนยันแล้ว: `FD-1`=Floor Drain, `DR090SLx`=รหัสประตู, `CT002P`/`C805P`/`C805S`=รหัสสินค้า COTTO — เพิ่ม `FD`/`DR` เข้า schema, เพิ่มโน้ตอธิบาย COTTO ในหมวด 3.5 ใหม่
- **ตรวจสอบไฟล์ DXF จริงเพื่อตอบคำถาม "COTTO code กำกับไว้ที่ไหน"** ด้วย ATTDEF/INSERT/spatial-proximity: พบว่า `CT002P` ไม่มี attribute อธิบายเลย, `C805P` มีแค่ ATTDEF serial number, ไม่มี text ไหนพิมพ์ชื่อ block ตรงๆ ในไฟล์ มีแค่ MTEXT ชื่อรุ่นแบบหลวมๆ วางใกล้ๆ (ไม่ผูก data) — **สรุปว่ารหัสประเภทนี้ไม่มีทาง auto-resolve จากไฟล์ CAD ได้ ต้องมี vocabulary ภายนอก**
- **พบเรื่องสำคัญกระทบสถาปัตยกรรม:** block จริง (`CT002P`, `C805P`, `DR090SL1`) ถูก insert อยู่บนเลเยอร์ `WALL`/`DIM` ที่ชื่อไม่เกี่ยวอะไรกับตัวมันเลย — ยืนยันว่า **ห้ามใช้ชื่อเลเยอร์ตัดสิน INSERT/block เด็ดขาด ต้องดูชื่อ block เท่านั้น** เขียนเป็น `entity_type_warning` ใน schema แล้ว (ดู [07-drawing-signal-vs-noise.md §12](./07-drawing-signal-vs-noise.md#12-รอบที่-3-ตรวจสอบ-insertattdefspatial-proximity-จริง--พบว่า-ชื่อเลเยอร์ที่-block-ถูก-insert-อยู่-เชื่อถือไม่ได้เลย))
- `B-1`/`B-2` ยืนยันว่าเป็น block definition ที่ไม่เคย insert ใช้งานเลย — ไม่ต้องจัดหมวด
- **ตอบคำถามใหญ่ "จะสอนไพน้อยไม่ให้ต้องแก้โค้ด/ลดภาระได้ยังไง":** ย้าย exclusion denylist ออกจาก `pynoi_parser.py` (เคย hardcode เป็น Python constant) เข้าไปเป็น `exclusion_rules` key ใน `04-category-schema.md` แทน — ตอนนี้ทั้ง Layer Exclusion และ Category Schema เป็นข้อมูล (JSON) ล้วนๆ ไม่มี keyword ไหนอยู่ในโค้ดเลย โหลดผ่าน `schema_loader.py` ที่สร้างใหม่
- สร้างระบบ `vocabulary_review.csv` + `apply_vocabulary_review.py`: `classify_layers.py` เขียนคำที่ไม่รู้จักลง CSV อัตโนมัติทุกครั้งที่รัน เจ้าของโปรเจกต์กรอกแค่ 3 คอลัมน์ (target_category/action/status) ในไฟล์ CSV เหมือนกรอก Excel แล้วรัน `apply_vocabulary_review.py` เพื่อ merge เข้า schema อัตโนมัติ — ไม่ต้องแตะ JSON/โค้ดเลยตลอดวงจร เขียนอธิบายเต็มใน [09-vocabulary-review-workflow.md](./09-vocabulary-review-workflow.md)
- ⚠️ **เหตุการณ์ระหว่างพัฒนา:** `apply_vocabulary_review.py` เวอร์ชันแรกใช้ `re.sub(pattern, new_json_string, text)` เขียนกลับไฟล์ ทำให้ backslash ในค่า regex pattern (`exclusion_rules.patterns`) หายไปครึ่งหนึ่งเพราะ Python ตีความ backslash ใน replacement string เป็น backreference syntax — ไฟล์ `04-category-schema.md` กลายเป็น JSON ที่ parse ไม่ผ่านชั่วคราว แก้ไขด้วยมือทันทีที่ตรวจพบ (ยืนยันด้วย round-trip test) แล้วเปลี่ยนโค้ดไปใช้ string slicing แทน re.sub — บันทึกไว้เป็นบทเรียนใน [09-vocabulary-review-workflow.md §7](./09-vocabulary-review-workflow.md#7-️-ข้อควรระวังที่พบระหว่างพัฒนา--ห้ามใช้-resubpattern-string-text-เขียนกลับ-json)

## 2569-08-23 (8)

`project/code/python/classify_layers.py` (ใหม่), `project/code/python/pynoi_parser.py`, `04-category-schema.md`, `07-drawing-signal-vs-noise.md`, `BACKLOG.md` — สแกนหาคำ/ตัวย่อที่ "ไพน้อย" ยังไม่รู้จักอย่างเป็นระบบ (ตอบคำถามเจ้าของโปรเจกต์ "ยังพบปัญหาตัวย่อ/คำที่ไม่รู้อีกไหม")

- เขียน `classify_layers.py` เทียบทุกเลเยอร์ (220, จาก layer table ทั้งหมด ไม่ใช่แค่ที่มี geometry) + ทุก block (132) ในไฟล์ `newhouse 2569.dxf` กับทั้ง Layer Exclusion denylist และ Category Schema พร้อมกัน แบ่งผลเป็น excluded/categorized/**unknown**
- รอบแรกพบ 114 เลเยอร์ + 111 block ที่ไม่ตรงทั้งสองฝั่ง — ไล่แก้เป็นรอบๆ จนเหลือ 73 เลเยอร์ + 67 block
- **พบรูปแบบขยะใหม่ 3 แบบที่ไม่เคยบันทึกมาก่อน** (เขียนลง [07-drawing-signal-vs-noise.md §11](./07-drawing-signal-vs-noise.md#11-รอบที่-2-รัน-classify_layerspy-เทียบทุกเลเยอร์block-กับ-exclusion--category-schema-พร้อมกัน--พบรูปแบบขยะใหม่-3-แบบ)):
  1. เลเยอร์ตั้งชื่อตามน้ำหนักเส้น (`0.15`, `0.9`, `05`, `100` ฯลฯ) — ไม่มีความหมายเชิงเนื้อหาเลย ต้องใช้ regex `^[0-9]+(\.[0-9]+)?$` ไม่ใช่ keyword
  2. ชื่อที่ซอฟต์แวร์สร้างเอง ไม่ใช่ฝีมือคน — AutoCAD anonymous block (`A$C...`), built-in block (`_Origin`, `_Open30` ฯลฯ), ปลั๊กอิน/tool prefix (`ADCADD_`, `AUDIT_`, `AVE_`) — เพิ่ม regex ใน `EXCLUDED_LAYER_PATTERNS`
  3. **ยืนยัน false positive จริงจาก keyword สั้น** ที่เคยเตือนไว้แค่ทฤษฎี — `AC` match ผิดเข้า `A$C69AC065E`, `DB` match ผิดเข้า `_ClosedBlank` (ตัวอักษร "dB" กลางคำ) — แก้ด้วย `_token_aware_match()` (keyword <4 ตัวอักษรต้องแมตช์เป็น prefix ของ token ไม่ใช่ substring ที่ไหนก็ได้ — ยังคงให้ `DIM` match `DIMENSION` ได้ปกติเพราะเป็น token เดียวกันจริง)
- เพิ่ม keyword ที่มั่นใจสูงเข้า `04-category-schema.md`: `CONCRETE`/`คอนกรีต`/`เหล็ก`/`ROOF` (หมวด 1), `PUMP`/`PIPE`/`CW PIPE`/`FLOOR DRAIN` (หมวด 3), `SWITCH`/`BREAKER`/`เบรกเกอร์` (หมวด 4)
- เพิ่ม `TREE`/`SHRUB`/`FURNITURE`/`CAR`/`HACTH`/`HATC` (typo ของ HATCH) เข้า Layer Exclusion denylist ใน `pynoi_parser.py`
- **เพิ่ม `pending_review` array ใน `04-category-schema.md`** (9 กลุ่ม) บันทึกทุกอย่างที่ยังไม่มั่นใจพอจะเดา แบ่งกลุ่ม: อาจเป็นหมวดที่ขาดไปทั้งหมด (ครัว, แก๊ส, งานภายนอก/ที่จอดรถ), likely เป็น library ต้นไม้ตกแต่ง (`TF-`/`TS-`/`PM-` prefix), likely เป็นรหัสสินค้า COTTO (`CT-`/`C0xx-` prefix ตรงกับ pattern ที่เจอใน ground-truth BOQ จริง), likely เป็นรหัสประตู (`DR0xxSLx`), กำกวมเรื่องชั้น/โครงสร้าง (`B-1`, `L1-L3`, `FD-1`), คลุมเครือเกินจะเดา (ต้องเปิดไฟล์ดูด้วยตา), และชื่อทดสอบไร้ความหมาย (`aaa`, `bbb` ฯลฯ)

## 2569-08-23 (7)

`04-category-schema.md`, `03-ai-boq-procedure.md`, `README.md`, `BACKLOG.md` — ยกระดับ category schema เป็น v2.0 (Master CAD-Parsing & Search Schema) ตามข้อเสนอของเจ้าของโปรเจกต์

- เจ้าของโปรเจกต์ทดลองให้ AI อ่าน BOQ หลายไฟล์ พบโครงสร้างคล้ายกัน เสนอ schema ใหม่ที่มี `keywords`/`cad_search_layers` ต่อหมวด/หมวดย่อย ไม่ผูกชื่อโครงการ รองรับ dynamic append — แทนที่ schema v1 (flat list ไม่มี keyword) ทั้งหมด
- ตรวจสอบ keyword/layer ที่เสนอมาเทียบกับชื่อเลเยอร์จริง 155 เลเยอร์ใน `newhouse 2569.dxf` แล้วเพิ่ม/แก้ตามช่องว่างที่พบจริง: `COLM`, `TRUSS`/`STEEL`, `WIND` (เลเยอร์จริงย่อจาก window), `WC`, `LAMP`, และเพิ่ม `cad_search_layers` ให้ครบทุกหมวด (ร่างเดิมมีแค่หมวด 1-3)
- **หลักฐานสำคัญที่พบ:** เลเยอร์จริงในไฟล์ชื่อ `ประตู-หน้างต่าง` (สะกดผิด มี ง เกินจาก "หน้าต่าง") — ยืนยันว่า exact substring matching ไม่พอในโลกจริง ต้องมี fuzzy/edit-distance matching ในอนาคต (backlog)
- **แก้ข้อขัดแย้งจริงที่พบ:** ร่างเดิมจัด "ถังบำบัด/บ่อพัก" ไว้หมวดโครงสร้าง (มุมมองทางกายภาพ) แต่ [08-ground-truth-boq-validation.md](./08-ground-truth-boq-validation.md) ยืนยันจาก BOQ จริงว่าถูกตีราคาในหมวดสุขาภิบาล — ย้าย keyword ไปหมวด 3 (สุขาภิบาล) ให้ตรงธรรมเนียมจริง เขียนหมายเหตุอธิบายเหตุผลไว้ในหมวด 1.5 ด้วย
- ชี้แจงชัดเจนในเอกสาร: schema นี้ (parsing/ค้นหา) กับ schema การรวมยอด BOQ จริง (บัญชี) เป็นคนละเรื่อง อนุญาตให้ไม่ตรงกันได้ — ปัญหา "Preparation เป็นหมวดใหญ่หรือไม่" จาก entry ก่อนหน้ายังไม่ถูกแก้ด้วย schema นี้ ยังคงเป็น open decision
- อัปเดต Step 6 ของ SOP ใน `03-ai-boq-procedure.md` ให้ชี้ไปที่ schema v2.0 และอธิบายลำดับการทำงานร่วมกับ Layer Exclusion (Step 1.5)

## 2569-08-23 (6)

`08-ground-truth-boq-validation.md`, `04-category-schema.md`, `project/code/python/read_ground_truth_boq.py`, `README.md`, `BACKLOG.md`, `requirements.txt` — ตรวจสอบ category schema กับ BOQ จริงที่เจ้าของโปรเจกต์ถอดแบบเอง

- เขียน `read_ground_truth_boq.py` (ใช้ `xlrd==1.2.0` เพราะไฟล์เป็น `.xls` เก่า, openpyxl อ่านไม่ได้) อ่านโครงสร้าง `BOQ - new house.xls` (6 ชีต: total, Struc, Arch, San, EE, ACC รวม 3,481,473.82 บาท)
- **หมวดสถาปัตย์ (3_Architecture) ตรงกับ schema เดิม 100%** รวมรหัส F1-F4/D1-D5/W1-W4 — schema เดิมน่าจะสร้างมาจากไฟล์นี้อยู่แล้วโดยตรง
- **พบ 3 จุดที่ schema ไม่ตรง/ไม่ครบ ใน `04-category-schema.md`:**
  1. `1_Preparation` ไม่ใช่หมวดใหญ่แยกในการคิดราคาจริง — ซ้อนเป็นหมวดย่อยอันดับ 1 ใน `2_Structure` (240,000 บาท จาก 1,246,337.80 ของหมวดโครงสร้าง) เพิ่ม ⚠️ callout ไว้ ยังไม่ตัดสินใจว่าจะรวมหรือแยก (ค้างเป็น backlog)
  2. หมวดไฟฟ้า: สายเมนกับตู้โหลดเซ็นเตอร์ถูกคิดราคาคนละหมวดย่อยจริง (ตู้โหลดเซ็นเตอร์ไปอยู่ในหมวด "แสงสว่าง" แทน) — แก้ wording ใน schema แล้ว
  3. หมวดปรับอากาศขาด 2 หมวดย่อย (อุปกรณ์ประกอบท่อ, อุปกรณ์ยึด/แขวนท่อ) — เพิ่มเป็น 6.3/6.4 แล้ว
- **ตอบคำถามเรื่อง "ปริมาณใกล้เคียงกันไหม" ตรงๆ ว่ายังเทียบไม่ได้จริง** — เพราะ `pynoi_parser.py` ยังไม่มี Net Deduction, ยังไม่ยืนยันสเกล, เลเยอร์ WALL ยังไม่ผ่าน Duplicate Purge/Bounding Box Filter (289,034 ตร.ม. vs ของจริง 300 ตร.ม. ก่ออิฐ) — บันทึกไว้ว่าต้องทำอะไรก่อนถึงจะเทียบได้อย่างมีความหมาย ไม่ใช่เทียบดิบๆ ตอนนี้
- **พบว่า BOQ ต้นฉบับเองก็มีรอยเปื้อนจากความเร่งรีบเหมือนกัน** — รหัสรายการซ้ำ (`2.1` ซ้ำใน Struc, `5.1` ซ้ำใน Arch) น่าจะเป็น typo ของผู้ทำ BOQ เอง (ตั้งใจจะเป็น `2.10`/`5.10`) — เขียนฟังก์ชัน `check_duplicate_codes()` ตรวจจับอัตโนมัติไว้ในสคริปต์

## 2569-08-23 (5)

`project/code/python/pynoi_parser.py`, `BACKLOG.md` — implement Layer Exclusion (กลไกแรกของ 3 กลไกใน CAD Noise Filtering Engine)

- เพิ่ม `EXCLUDED_LAYER_KEYWORDS` (substring, case-insensitive) + `EXCLUDED_LAYER_EXACT` (exact match สำหรับเลเยอร์ `0`) และฟังก์ชัน `is_excluded_layer()` ตาม [07-drawing-signal-vs-noise.md §5.2](./07-drawing-signal-vs-noise.md#5-️-กลไกที่ต้องสร้าง-ระบบคัดกรองขยะ-cad-cad-noise-filtering-engine)
- `report_takeoff_primitives()` แยกผลลัพธ์เป็น 2 ส่วนชัดเจน: INCLUDED กับ EXCLUDED (ไม่ซ่อนของที่ตัดทิ้ง — พิมพ์ให้เห็นเพื่อ audit false positive/negative ได้เสมอ)
- เพิ่ม CLI flag `--extra-exclude` ให้เพิ่มคำที่ต้องตัดทิ้งเฉพาะไฟล์นั้นๆ ได้ โดยไม่ต้องแก้โค้ด
- **รันจริงกับ `newhouse 2569.dxf` อีกครั้ง — ผลลัพธ์:** ตัดออกไป 16 เลเยอร์ ครอบคลุมตัวการ์ตูนขยะที่ใหญ่ที่สุด 2 ตัว (`Bodr A3` 39,299 ตร.ม., `DEFPOINTS` 113,955 ตร.ม.) รวมถึง `GRID`/`DIM*`/`TITLE`/`TABLE`/`TEXT`/`SPEC`/เลเยอร์ `0`
- **ข้อค้นพบสำคัญที่ยืนยันสมมติฐานเดิม:** Layer Exclusion อย่างเดียว **ไม่พอ** — เลเยอร์ `WALL` (ชื่อถูกต้อง ไม่ใช่ขยะ) ยัง sum พื้นที่ได้ 289,034 ตร.ม. เท่าเดิม เพราะปัญหาอยู่ที่มี hatch/detail loop ปนอยู่ *ในเลเยอร์เดียวกัน* กับผนังจริง — ต้องทำ Duplicate Purge + Bounding Box Filter ต่อ (ยังไม่ implement)
- พบเลเยอร์ `05` (area 100,116 — ใกล้เคียงขนาด `Bodr A3`) ที่ denylist ปัจจุบันจับไม่ได้ เพราะตั้งชื่อเป็นตัวเลขล้วน — ค้างเป็น backlog ให้ตรวจสอบด้วยตา

## 2569-08-23 (4)

`07-drawing-signal-vs-noise.md`, `03-ai-boq-procedure.md`, `02-business-model-finance.md`, `BACKLOG.md` — ขยายความ taxonomy ขยะ CAD ด้วยกรอบคิดเชิงพฤติกรรมของดราฟต์แมน (ไม่ผูกกับไฟล์ใดไฟล์หนึ่ง)

- เพิ่ม §4 "4 ประเภทขยะในการเขียนแบบ" (เส้นช่วยวาด/เล็งแนว, hatch pattern, กรอบกระดาษ/viewport/note รอบนอก, เส้นซ้อนทับ/ซากแบบเก่า) — เป็นข้อเสนอจากเจ้าของโปรเจกต์ในฐานะ QS มืออาชีพ คนละชุดกับ §3 ที่เป็นหลักฐานเฉพาะไฟล์ newhouse
- เพิ่ม §5 "CAD Noise Filtering Engine" — กลไก 3 อย่างที่ต้องสร้างจริงก่อนประมวลผล: Bounding Box Filter, Layer Exclusion (denylist คำสำคัญ), Duplicate Purge (แนวทางคล้าย AutoCAD OVERKILL)
- เอกสารถูก renumber จาก 8 หัวข้อเป็น 10 หัวข้อ — แก้ cross-reference anchor ในไฟล์อื่นที่เคยชี้ไป §5/§6/§7/§8 เดิมให้ตรงกับเลขใหม่ (§7/§8/§9/§10) แล้ว

## 2569-08-23 (3)

`07-drawing-signal-vs-noise.md`, `03-ai-boq-procedure.md`, `02-business-model-finance.md`, `README.md`, `BACKLOG.md` — วิเคราะห์กรอบคิด "สัญญาณ vs ขยะ" ในไฟล์ CAD จากการรัน `pynoi_parser.py` จริง

- เขียน [07-drawing-signal-vs-noise.md](./07-drawing-signal-vs-noise.md): taxonomy ของขยะที่พบจริงในไฟล์ (Bodr A3/title block, DEFPOINTS, DIM family, GRID, layer `0`, block ตกแต่ง เช่น Human_01/corolla/BMW 820 oben/Shrub 4/TREE_P*), taxonomy ของสัญญาณจริง (Legend/Schedule table ที่มนุษย์เขียนไว้ = แหล่งความจริงที่น่าเชื่อถือที่สุด ไม่ใช่เรขาคณิตดิบ), และ heuristic 5 ข้อที่ใช้แยกได้จริง
- เพิ่ม **Step 1.5 (Drawing Readiness Assessment)** เข้าไปใน SOP ที่ `03-ai-boq-procedure.md` — ก่อนคำนวณปริมาณใดๆ ต้องจัดกลุ่มเลเยอร์/block เป็นสัญญาณ/ขยะที่รู้จัก/ไม่แน่ใจก่อนเสมอ
- **การตัดสินใจธุรกิจสำคัญ (ข้อเสนอจากเจ้าของโปรเจกต์):** เปลี่ยนปัญหา "ไฟล์ CAD สกปรก/ไม่มีมาตรฐาน" จากข้อจำกัดทางเทคนิคให้เป็นรายได้เพิ่ม — เพิ่มรายการรายได้ที่ 4 "CAD Cleanup Fee" ใน `02-business-model-finance.md` (ถ้า Drawing Readiness Report พบว่าไฟล์ไม่ผ่านเกณฑ์ เสนอบริการแก้ไข/จัดระเบียบแบบเป็นรายการแยกต่างหาก)

## 2569-08-23 (2)

`.gitignore`, `project/code/python/pynoi_parser.py`, `project/code/python/requirements.txt`, `BACKLOG.md` — เขียนพาร์เซอร์ DXF ต้นแบบตัวแรก

- แก้ `.gitignore` ที่ผิดพลาดจากรอบก่อน (เคย ignore `project/` ทั้งโฟลเดอร์ ทำให้ไฟล์ CAD อ้างอิงจริงและสคริปต์เดิม `cad_analyzer-1.py` ไม่ถูก track) — เปลี่ยนเป็น ignore เฉพาะไฟล์ไบนารีหนักที่ไม่จำเป็น (`*.dwg`, `*.skp`, `*.pdf`) ยังคง track `.dxf`/`.xls` ที่ใช้ parse/calibrate จริง
- ค้นพบโปรเจกต์ ground-truth ที่สอง **"welcome maerem" (Kadfarang)** ที่ไม่เคยถูกบันทึกในเอกสารชุดนี้มาก่อน มีทั้ง DXF และ BOQ Excel จริง (`RPS-BOQ-V1-24-3-68.xls`) — ค้างเป็น backlog ให้เขียน ground-truth doc เหมือน `ground-truth-newhouse-2569.md`
- เขียน `pynoi_parser.py` (ตั้งชื่อไฟล์แบบคงที่ ไม่ใช้ timestamp ในชื่อไฟล์ เพราะใช้ git commit เป็นตัวบันทึกประวัติเวอร์ชันแทน) — รายงาน DXF header/$INSUNITS, layer summary, block definitions, และ length/area ต่อเลเยอร์ (LINE + LWPOLYLINE) ต่อยอดจาก `cad_analyzer-1.py` เดิมที่สแกน FL-code เฉพาะโปรเจกต์ welcome maerem
- **รันจริงกับ `newhouse 2569.dxf` (21MB) สำเร็จ** — พบว่าไฟล์มี 155 เลเยอร์ที่ตั้งชื่อไม่เป็นมาตรฐาน (ตรงกับความเสี่ยงที่เคยเขียนเตือนไว้ใน `01-market-competitors.md`) และการ sum พื้นที่รูปปิดทั้งหมดต่อเลเยอร์แบบ naive ใช้ไม่ได้จริง (เลเยอร์ `WALL` ได้ 289,034 ตร.ม. ซึ่งไร้ความหมายสำหรับบ้านหลังเดียว) — ต้องคิด logic แยกเส้นขอบห้อง/ผนังจริงออกจาก detail loop อื่นก่อน จึงจะคำนวณ BOQ ได้ ดู backlog

## 2569-08-23

`README.md`, `CLAUDE.md`, `BACKLOG.md`, `00-overview.md`, `01-market-competitors.md`, `02-business-model-finance.md`, `05-technical-architecture.md`, `06-roadmap.md` — สร้างโครงสร้างเอกสารชุดแรกของโครงการ (ก่อนหน้านี้มีแค่ 4 ไฟล์ร่างใน Google Drive)

- ตั้ง git repo แยกที่ `D:\AI Estimation - Modeling Platform` (ย้ายจาก `G:\My Drive\project\markdown\AI Estimate\` เพื่อเลี่ยงความเสี่ยง sync client ของ Google Drive ตัดขัดจังหวะการเขียนไฟล์ .git)
- ยืนยันชื่อ AI agent = "ไพน้อย (Py-Noi)" — ส่วนชื่อโครงการ/แพลตฟอร์มยังไม่ตัดสินใจ (เดิมเอกสารเรียกสลับ "BuildFlow AI" กับ "ไพน้อย" ปนกัน แยกให้ชัดแล้วว่าคนละชื่อกัน คล้ายกับ "รักบ้าน" (โครงการ) กับ "มะลิ" (AI assistant) ของรักบ้าน)
- ทำวิจัยคู่แข่งเบื้องต้น (ไทย + โลก) พบว่ายังไม่มีใครทำ AI auto-BOQ ที่ localize ราคากลางไทย — ดู [01-market-competitors.md](./01-market-competitors.md)
- ย้ายไฟล์เดิม 4 ไฟล์: `00-readme.md` → `ground-truth-newhouse-2569.md`, `01-overview.md` → `01-overview-draft-old.md` (เก็บไว้อ้างอิง), `02-Ai-BOQ Procedure.md` → `03-ai-boq-procedure.md`, `03-structure-cad.md` → `04-category-schema.md`
- แผนสถาปัตยกรรม: web app หน้าบ้าน + core engine อยู่บน server ทั้งหมด (กันการ reverse engineer) เริ่มรันบนเครื่อง dev ก่อนในเฟสนี้ — บันทึกข้อจำกัดจริงของแนวทางนี้ไว้ใน [05-technical-architecture.md §2](./05-technical-architecture.md#2-ข้อควรระวัง--server-side-core-ป้องกัน-reverse-engineering-ได้แค่ระดับหนึ่ง-ไม่ใช่ทั้งหมด)
